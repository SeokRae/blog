# Bundler cache
.bundle
vendor

# Jekyll cache
.jekyll-cache
.jekyll-metadata
.sass-cache
_site

# RubyGems
*.gem

# Blog pipeline 작업 파일 (발행은 _posts/로만 이뤄짐).
# 단 리서치 노트는 발행된 주장의 근거 사슬이므로 추적한다 — 본문의 외부 인용을
# 되짚을 수 있어야 한다 (#16). 디렉터리째 무시하면 하위 파일을 되살릴 수 없으므로 `_drafts/*`.
_drafts/*
!_drafts/*.research.md
.claude/session-plan.md

# NPM dependencies
node_modules
package-lock.json

# IDE configurations
.idea
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
!.vscode/tasks.json

# Misc
_sass/vendors
assets/js/dist

# Hidden system files
*~
.DS_Store
Thumbs.db
