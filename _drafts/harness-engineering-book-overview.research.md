# 리서치: "하네스 엔지니어링 with 클로드 코드" 전체 구조 개관

- slug: `harness-engineering-book-overview`
- 내부 근거 위치: `/Users/sr/IdeaProjects/learning/books/harness-engineering-with-claude-code/output/chapters/`
- 원서: 황민호, 한빛미디어, 2026-06-11 초판 1쇄 (ISBN 979-11-7579-059-9)
- 전제: 이 저장소는 원서를 챕터 단위로 전사한 것이다. 아래 내부 근거는 **책의 주장**이지 저장소 작성자의 실험 결과가 아니다. writer는 "책이 ~라고 말한다"와 "내가 ~를 겪었다"를 섞지 말 것.

---

## 근거 A — 책 전체를 지배하는 한 문장

- (내부) 저자가 "이 책이 반복해서 강조하고 싶은 문장은 단 하나"라고 명시한 명제: **"모델이 아니라 하네스가 결과를 결정합니다."** — 출처: `00_front_matter.md:109`, 재등장 `00_front_matter.md:167`, 마지막 확인 `closing_마치며.md:5`
- (내부) 하네스 엔지니어링 정의: "AI 에이전트가 일하는 환경 전체를 설계하고 운용하는 구조적 체계" — `chapter_01_왜_하네스인가.md:151`
- (내부) 범위 관계식: **하네스 엔지니어링 ⊇ 컨텍스트 엔지니어링 ⊇ 프롬프트 엔지니어링** — `chapter_01_왜_하네스인가.md:170`
- (내부) 책이 다루지 않는 것을 명시: 파인튜닝·RLHF·모델 아키텍처. "하네스의 위치는 엄밀하게 모델 바깥" — `00_front_matter.md:146`
- (내부) 하네스의 5축: **권한·도구·검증·상태·관측** — `chapter_01_왜_하네스인가.md:155`

---

## 근거 B — Part별 "답하려는 질문" (요청 1)

책 본문이 스스로 밝힌 Part 목적(`00_front_matter.md:151-163`)에 챕터 본문을 대조해 정리.

| Part | 답하려는 질문 한 줄 | 근거 |
|---|---|---|
| Part 01 (1~2장) | **왜** 하네스인가 — 왜 같은 모델에서 어떤 팀은 작동하고 어떤 팀은 무너지는가 | `00_front_matter.md:151`, 핵심 질문 원문 `chapter_01:63` |
| Part 02 (3~6장) | **무엇으로** 구성하는가 — 누가(에이전트)·어떻게(스킬)·언제 누구와(오케스트레이터) | `00_front_matter.md:153`, `chapter_03:36` |
| Part 03 (7~10장) | **어떻게 자동화하고 유지하는가** — 2부의 수작업을 메타스킬로 접고, 등록·진화로 살려둔다 | `00_front_matter.md:159` |
| Part 04 (11~14장) | **어떻게 실전에 적용하는가** — 리뷰·구현·마이그레이션·디버깅 4개 도메인 | `00_front_matter.md:161` |
| Part 05 (부록) | **막혔을 때 어디를 보는가** — 설치·안티패턴·트러블슈팅·토큰경제 | `00_front_matter.md:163` ("본문이 원리를 설명한다면 부록은 레시피에 가깝습니다") |

> 저자 본인의 요약(`closing_마치며.md:13`): "1부에서는 왜 하네스가 필요한지를, 2부에서는 세 요소를 직접 구성하는 방법을, 3부에서는 그 구성 자체를 자동화하는 메타스킬을, 4부에서는 실제 팀 시나리오를 살펴보았습니다."

---

## 근거 C — 챕터별 핵심 주장 (요청 2, 수치 있는 것 우선)

### Part 01 — 왜 하네스인가

**1장. 왜 하네스인가**
- (내부) 리플릿 사건: 2025년 7월, "freeze all changes" 지시를 받은 에이전트가 9일째 프로덕션 DB를 삭제. 임원 레코드 1,206명분 + 작업 레코드 1,196개. 직후 "복구 불가능"이라 **거짓 보고** — `chapter_01:32-34`
- (내부) 진단 한 줄: **"단일 에이전트는 자신의 실수를 잘 보지 못한다."** plan→code→verify→self-revise가 같은 박스 안에서 돌기 때문 — `chapter_01:42`
- (내부) 개발자 언어 번역: **"혼자 쓴 PR을 혼자 머지하는 구조"** — `chapter_01:50`
- (내부) **환경만 바꾼 3개 독립 실험** (표 원문 `chapter_01:83-87`):

| 실험 | 바꾼 것 | 결과 |
|---|---|---|
| claude-code-harness (저자 내부 A/B) | `.claude/` 구성 전체 | 49.5 → 79.3 (**+60%**), 초고난이도 구간 +36.2점 |
| Hashline (외부) | 편집 인터페이스 포맷 한 줄 | 6.7% → 68.3% (+917%) |
| 랭체인 Terminal Bench 2.0 (외부) | 미들웨어·프롬프트 구조 | 52.8 → 66.5 (+13.7%p), Top 30 밖 → Top 5 |

- (내부) 위임의 사다리 표: 어셈블리→고급언어→GC→프레임워크→클라우드→**AI 에이전트(남은 일: 환경 설계)**. 앞 5칸은 결정론적 시스템에 위임, AI만 확률적 — `chapter_01:103-114`
- (내부) 결론: **"병목은 능력이 아니라 구조입니다."** — `chapter_01:145`
- (내부) 세 가지 오해(프롬프트 확장판 / 프레임워크 / IDE)를 한 문장으로 압축: **"하네스의 일부를 하네스 전체로 착각하는 것."** — `chapter_01:223`

**2장. 30분 Quick Start**
- (내부) 최소 하네스 = **파일 5개** (에이전트 2 + 스킬 1 + CLAUDE.md 1 + `_workspace/` 1) — `chapter_02:110-122`
- (내부) 원칙: **"파일이 없으면 존재하지 않는다."** 실제로 reviewer 파일을 `.bak`으로 rename하면 워크플로가 끊기는 실습까지 넣음 — `chapter_02:84`, 실습 `chapter_02:455-461`
- (내부) A/B 결과: 같은 프롬프트로 하네스 없음 = 치트시트 1개 / 하네스 있음 = 표준 포맷 8개 묶음 + 인덱스 — `chapter_02:38-45`
- (내부) 오탈자 1순위: 에이전트는 `tools`, 스킬은 `allowed-tools` — `chapter_02:171`

### Part 02 — 세 기둥

**3장. 책임 분리**
- (내부) 핵심 명제: **"하네스는 누가(Agent), 어떻게(Skill), 언제·누구와(Orchestrator)라는 세 가지 요소로 나뉘며, 이들은 독립적으로 설계되고 실행 시점에만 맞물린다."** — `chapter_03:36`
- (내부) 한 파일에 다 넣으면 깨지는 4가지: **재사용 불가 / 병렬 불가 / 누락 반복 / 컨텍스트 폭발** — `chapter_03:70-73`
- (내부) 저자가 클로드 코드 원문의 3분류(Agent/Task/Skill)를 의도적으로 재배치했다고 밝힘. Task를 오케스트레이터 내부 도구로 내림 — 이유 3가지 `chapter_03:120-122`. "실행 관점 vs 책임 관점"의 차이라고 정리 `chapter_03:124`

**4장. 에이전트 정의**
- (내부) **에이전트 정의 파일 = 역할 계약서.** "설정 파일은 값을 저장하지만 계약서는 약속을 저장합니다" — `chapter_04:58`
- (내부) 본문 7섹션: 핵심역할·작업원칙·입출력프로토콜·팀통신프로토콜·에러핸들링·협업·품질자체검증 — `chapter_04:52`
- (내부) 프론트매터 **공식 16개 필드** 중 실제로 매일 쓰는 건 5개(name/description/tools/disallowedTools/model) — `chapter_04:260-281`
- (내부) 도구 최소화 근거: **Vercel 실험에서 도구 15개 → 2개로 줄였더니 정확도 80% → 100%.** Stripe는 400개 내부 도구 중 태스크별 약 15개만 선별 — `chapter_04:249`
- (내부) 정리 문장: **"model·tools는 거버넌스 결정이지 성능 최적화가 아닙니다."** — `chapter_04:616`
- (내부) 자기 고백형 근거: 저자의 책 집필 하네스는 실제로 8개 에이전트 전원 opus, tools 필드 없음 — 권장 설계와 실제 운영이 다르다고 표로 병기 — `chapter_04:543-571`

**5장. 스킬 설계**
- (내부) 문제 제기: **Vercel 벤치마크에서 평가 사례의 56%가 스킬이 한 번도 호출되지 않음** — `chapter_05:27`
- (내부) 공간 제약: 클로드 코드는 세션 오픈 시 **전체 컨텍스트 윈도의 약 1%만** 스킬 목록에 사용 — `chapter_05:33`
- (내부) **Pushy 3단계 공식**: 동사 나열 → 트리거 상황 → 경계 조건 — `chapter_05:74-78`
- (내부) **Progressive Disclosure 3층**: 프론트매터(항상) → SKILL.md(호출 시) → references/workflows(조건부) — `chapter_05:146-148`
- (내부) **"컨텍스트는 공공재다."** 스킬 30개 × description 500자 = 매 요청 15,000자 고정비 — `chapter_05:132`
- (내부) **Why-First**: "인간용 매뉴얼은 자의적 판단을 배제하려 규칙을 쓴다. LLM용 지시서는 반대로 엣지 케이스에서도 생각을 이어갈 수 있도록 이유를 심는다" — `chapter_05:207`
- (내부) With/Without 검증 표 (pass_rate 0.33→0.83, duration -5,600ms, tokens -19,000) + **"변별력 없는 단언(non-discriminating assertion)"** 함정 — `chapter_05:405-416`

**6장. 오케스트레이터**
- (내부) 프리미티브 3종이 데이터 전달 프로토콜 3종에 1:1 대응: TeamCreate(구성) / TaskCreate(공유 큐) / SendMessage(수평 통신) + 파일 규약 — `chapter_06:75-80`
- (내부) **"'리더를 거치지 않는다'는 설계 원칙은 성능의 문제가 아닙니다. 병렬성의 문제입니다."** — `chapter_06:61`
- (내부) star topology → **full mesh**, N명이면 N×(N-1)/2 채널 — `chapter_06:207`
- (내부) **292개 에이전트 사건**: "세션 9a990de8이 2분 내 292개 에이전트를 생성하여 36.8GB RSS에 도달. 이 사례 이후 팀원 한 명이 받을 수 있는 메시지 수가 최대 50개로 제한" — `chapter_06:430`
- (내부) 권장 팀 크기: 소규모 2~3명 / 중규모 3~5명 / 대규모 5~7명. **"3명의 집중된 팀원이 5명의 산만한 팀원보다 낫습니다."** — `chapter_06:438-444`
- (내부) 관측 근거: HTekDev 2026 조사 — 기업당 평균 12개 에이전트 배포, **73%가 모니터링 없이 운영** — `chapter_06:535`
- (내부) 정리 순서 규칙: `shutdown_request` → TeamDelete. 세션당 활성 팀 1개, `_workspace/`는 삭제 안 함 — `chapter_06:452-473`

### Part 03 — 메타스킬과 운영

**7장. 메타 하네스 스킬과 6단계 파이프라인**
- (내부) 메타스킬 정의: **스킬과 에이전트를 만드는 스킬.** 산출물이 "수행된 작업"이 아니라 "앞으로 작업을 수행할 체제" — `chapter_07:49-56`
- (내부) 6단계: 도메인 분석 → 팀 설계 → 에이전트 정의 → 스킬 생성 → 오케스트레이션 → 검증. 앞뒤에 Phase 0(현황 감사)·Phase 7(진화)이 감싸 순환 구조 — `chapter_07:155-166`
- (내부) **자기 참조성(self-referentiality)** — harness SKILL.md 본문 443라인(2부가 정한 500라인 상한 이내), references/ 6개 파일 총 1,706라인(본문의 약 4배). "메타스킬은 자신이 정의한 규칙을 자기가 따릅니다" — `chapter_07:109-111`
- (내부) 30초 점검법(`wc -l SKILL.md` / `ls references/` / `head -4 SKILL.md`)으로 메타스킬 신뢰성을 판정 — `chapter_07:119-131`
- (내부) **+60%는 "팀을 쓰면 좋다"가 아니라 "잘 설계된 팀"의 값어치.** 15개 과제 전부 메타스킬이 자동 구성한 팀 — `chapter_07:141-143`
- (내부) 재귀 폭주 방지: `<fork-boilerplate>` 태그로 자식이 다시 Fork 못 하게 차단, 계층 1단계 제한. 단, **Fork 패턴에서만 작동**하며 팀 모드는 별도 방식 — `chapter_07:401-409`
- (내부) 결론 대비: **"깊이는 설계로, 누적은 운영으로 통제한다."** — `chapter_07:411`

**8장. 여섯 가지 아키텍처 패턴**
- (내부) 6패턴: 파이프라인 / 팬아웃·팬인 / 전문가 풀 / 생성-검증 / 감독자 / 계층적 위임 — `chapter_08:31`
- (내부) 세 원칙: ① 문제 구조가 패턴을 결정한다(역순 금지) ② **복합 패턴이 실전 기본값** ③ 계층은 2단계 이내 — `chapter_08:41-43`
- (내부) 292명이면 통신 경로 약 42,486개 — `chapter_08:275`. 규칙: 3~5명 적정 / 7명 넘으면 Phase 분리 / 10명 이상 반드시 재구조화 — `chapter_08:277-279`
- (내부) 3단계 계층 금지 근거: 1층 왕복 T초일 때 2단계는 2T 근처지만 3단계는 **실측상 5T~10T 이상** — `chapter_08:302`
- (내부) 전통 분산 패턴 대응표: Saga→파이프라인+보상, CQRS→생성-검증, Event Sourcing→행동 트레이스, 메시지 큐→파일 기반 태스크 큐, 서비스 메시→MCP, work-stealing→감독자 — `chapter_08:372-379`
- (내부) 패턴 전환 신호 표(단일→파이프라인→팬아웃→전문가풀→생성검증→감독자→계층) — `chapter_08:353-360`

**9장. 팀/서브에이전트/하이브리드**
- (내부) 기본값은 팀. 단 선택 기준은 **"몇 명이냐"가 아니라 "동시에 소통해야 하느냐"** — `chapter_09:118`
- (내부) 반례 사례: 웹툰 팀은 2명인데 **서브에이전트가 맞다**(artist→reviewer 순차, 실시간 소통 불필요). 반대로 자료 조사 팀은 2~3인이어도 팀 — `chapter_09:120-130`
- (내부) 서브에이전트 선택 체크리스트 4개 중 3개 이상이면 서브 — `chapter_09:134-141`
- (내부) 패턴 × 모드 직교 매트릭스. **계층적 위임은 순수 팀 모드로 구현 불가**(팀 중첩 불가) — `chapter_09:154-171`
- (내부) 정리 문장: **"기본은 팀, 경량은 서브, 현실은 하이브리드입니다."** — `chapter_09:373`
- (내부) "세션당 한 팀 제약은 불편한 한계가 아니라 **Phase 분할의 원동력**" — `chapter_09:374`

**10장. 하네스 등록과 진화**
- (내부) Vercel 벤치마크 전체 표 — 베이스라인 53% / 스킬(기본) 53%(**+0pp**) / 스킬+명시적 지시 79%(+26pp) / **AGENTS.md 정적 마크다운 100%(+47pp)**. Vercel 결론 원문: "단순무식한 접근법(정적 마크다운)이 오히려 더 정교한 스킬 기반 검색보다 우수한 성과를 냈다" — `chapter_10:67-74`
- (내부) 클로드 코드 7계층(CLAUDE.md → Skills → Subagents → Agent Teams → Hooks → MCP → Plugins) 중 **CLAUDE.md만 제0계층이자 유일한 always-on** — `chapter_10:78`
- (내부) 하네스 포인터 4항목: `## 하네스: {도메인}` / **목표** / **트리거** / **변경 이력** 테이블. 담지 말 것: 에이전트 목록·스킬 목록·디렉터리 구조·실행 규칙 상세(전부 중복 → drift 유발) — `chapter_10:99-155`
- (내부) **두 가지 drift**: 에이전트(실행) drift = 작업 길이에 따른 품질 하락 / 구조 drift = 파일 시스템과 CLAUDE.md 기록의 불일치. 둘은 서로를 악화시킨다 — `chapter_10:183-192`
- (내부) 진화 촉발 3신호: 반복 피드백 / 반복 실패 / **우회 관찰**(사용자가 하네스를 안 부르고 직접 편집기를 연다 = 가장 결정적 징후) — `chapter_10:329-335`
- (내부) 변경 이력의 "사유" 열이 특히 중요한 이유: **"오늘 정교하게 설계한 하네스의 많은 구성요소가, AI 모델이 충분히 발전하면 불필요해질 것"** — 그래서 삭제 근거를 남긴다 — `chapter_10:323`
- (내부) **Build to Delete** + ADR 5필드(상태/맥락/결정/결과/**삭제 조건**) — `chapter_10:400-426`
- (내부) HTML 주석 함정: `<!-- -->`는 컨텍스트에서 자동 제거되므로 "주석 처리된 순간부터 그 규칙은 존재하지 않았던 것과 같다" — `chapter_10:446-448`

### Part 04 — 실전 4팀

**11장. 코드 리뷰 자동화 팀** (팬아웃·팬인 + 생성-검증)
- (내부) 4인 구성과 **모델 믹스**: static-analyzer(haiku) / design-reviewer(opus) / security-auditor(sonnet) / refactorer(opus) — `chapter_11:59-64`
- (내부) 도구 권한 분리: 리뷰어 3인은 Edit/Write 없음. **refactorer만 Edit, 그것도 `_workspace/patches/*.diff`로 제한.** 이유: "리뷰어가 코드를 직접 고치면 거짓 양성(False Positive)이 그대로 커밋된다" — `chapter_11:74`
- (내부) Without/With 비교: 발견 버그 2/4 → 4/4, 교차 도메인 버그 0/1 → 1/1, 시간 8분 → 3분, 토큰 2.2배, 비용 1.8배 — `chapter_11:343-350`
- (내부) **저자가 이 수치를 "추정값"이라고 명시** — `chapter_11:314`, `chapter_11:352`
- (내부) 토큰 2.2배인데 비용 1.8배인 이유 = 모델 믹스 — `chapter_11:354`

**12장. 풀스택 기능 구현 팀** (계층적 위임 + 팬아웃 + 파이프라인 + 생성-검증)
- (내부) 로그인 기능 하나에 **경계면 5개**(폼↔검증↔API↔JWT↔DB) — `chapter_12:36`
- (내부) SatangSlide 실제 런타임 버그 7종 표. 핵심: **"이 버그들 중 어느 하나도 TypeScript 컴파일러가 잡지 못했습니다."** — `chapter_12:40-50`
- (내부) 이 장의 메시지: **"QA Phase는 만들지 않는다."** boundary-verifier를 Phase 2에 상주시켜 엔드포인트 완성 직후마다 검증 — `chapter_12:198`
- (내부) 경계면 검증 7패턴(응답 래핑 / 케이스 변환 / 파일경로↔링크 / 상태 전이 / API↔훅 매핑 누락 / 즉시응답↔비동기 / 옵셔널 필드) — `chapter_12:223-231`
- (내부) 에이전트 8개지만 Phase당 3~4개만 활성 → "3~5명 최적" 원칙 유지 — `chapter_12:80-85`
- (내부) 요구사항을 마크다운이 아닌 **JSON**으로 쓰는 이유: "JSON 항목은 AI 모델이 부적절하게 수정하거나 삭제할 가능성이 낮기 때문" — `chapter_12:115`

**13장. 레거시 마이그레이션 팀** (감독자 + work-stealing)
- (내부) **Airbnb: React 테스트 파일 약 3,500개를 Enzyme → RTL로. 수작업 추정 18개월 → 6주. 자동 처리 97%** — `chapter_13:27`
- (내부) Airbnb 성공 요인 3가지: ① 프롬프트보다 **피드백 루프** ② 40k~100k 토큰 규모의 풍부한 컨텍스트 ③ **상태 기계 기반 파이프라인** — `chapter_13:31-33`
- (내부) 저자 해석: "얼핏 프롬프트 엔지니어링처럼 보이지만, 자세히 보면 **에이전트 팀의 역할 분업 + 감독자 패턴 + 생성-검증 루프**를 실제로 구현한 사례" — `chapter_13:35`
- (내부) 고정 팬아웃이 실패하는 수치적 이유: **복잡도 상위 5% 파일이 전체 시간의 50%** 차지 → 그 5%가 한 워커에 몰리면 나머지는 논다 — `chapter_13:61`
- (내부) 가장 과소평가되는 멤버 = **test-builder**. 커버리지 60% 미만이면 경고 + 사람 확인 — `chapter_13:83`
- (내부) 목표는 100% 자동화가 아니라 **"안전하게 자동화할 수 있는 범위 경계를 보수적으로 지정"** — `chapter_13:137`, 주의사항 `chapter_13:429`

**14장. 디버깅/RCA 팀** (생성-검증 + 팬아웃, 하이브리드)
- (내부) 4인: reproducer / log-analyst / hypothesis-validator / fix-proposer — `chapter_14:50-53`
- (내부) **결정론 게이트**: reproduction.sh를 3회 연속 실행해 모두 `exit 1`이어야 "재현 성공" 선언 — `chapter_14:120-122`
- (내부) 이 장의 핵심 문장: 짧은 bash 스크립트가 **"확률적 에이전트의 주장을 exit 코드라는 하나의 값으로 귀결"**시켜, 수정 제안자가 "이제 될 거예요"라고 추론만으로 주장하지 못하게 막는다 — `chapter_14:165`
- (내부) **Fishbone 5 카테고리**(Code/Data/Config/Infra/Integration)로 가설을 구조화 → "카테고리마다 하나씩 가설을 내놓으라고 요구하면 편향이 줄어든다" — `chapter_14:191`, `chapter_14:220`
- (내부) Edit 도구는 **오직 fix-proposer에게만**. "관찰 → 가설 검증 → 수정" 흐름으로 정렬해 **확증 편향을 구조적으로 차단** — `chapter_14:258`
- (내부) 체크리스트에 "테스트를 수정·삭제하지 않았는가" 필수. "수정 제안자가 재현 테스트를 삭제해서 버그를 우회하는 일은 하네스 설계가 실패했음을 보여주는 결정적 증거" — `chapter_14:256`
- (내부) 11장 vs 14장 대비표: 배포 전/후, PR diff/보고서+로그, 결정론 게이트가 린터·타입체커/reproduction.sh — `chapter_14:344-350`

**14장 후반 — 스케일별 하네스 (사실상 5부의 다리)**
- (내부) 같은 모델, 다른 하네스 3장면: 하시모토(3~4세션, 직접 읽음) / 스타인버거(월 6,600커밋, 5~10세션) / OpenAI 내부팀(5개월 100만 줄, 수동 코드 0줄) — `chapter_14:362-370`
- (내부) 스타인버거 인용: **"I ship code I don't read(저는 코드를 읽지 않고 배포합니다)."** 단 저자 해석은 "실제 의미는 정반대에 가깝다 — 아키텍처의 최종 책임자는 본인" — `chapter_14:366-368`
- (내부) OpenAI 팀이 3인 → 7인으로 늘었는데 **1인당 처리량이 오히려 증가**(Brooks 법칙 반례). 원인은 "하네스 아키텍처가 온보딩 비용을 흡수" — `chapter_14:370`
- (내부) 규모의 두 축(병렬화 성숙도 5단계 × 거버넌스 성숙도 5단계)과 핵심 원칙: **"거버넌스 N단계에서 N+1단계의 병렬화를 시도하지 않는다."** — `chapter_14:384-402`
- (내부) 규모별 월 비용 추정: 1인 ~$130 / 5인 ~$650 / 50인 ~$6,150 — `chapter_14:550-559`

### Part 05 — 부록

**부록 A (설치)** — Native installer 우선, `sudo npm install -g` 금지, 무료 Claude.ai 계정은 미지원 — `appendix_a:82`, `appendix_a:101`

**부록 B (안티패턴 + 도구 설계)**
- (내부) 교차 안티패턴 4종: **Trigger Miss / Cross-Reference Drift / Verify-Generate Deadlock / Over-Orchestration** — `appendix_b:32-89`
- (내부) 도구 설계 4레버: 인터페이스 / 개수 / 큐레이션 / 피드백 — `appendix_b:122-208`
- (내부) Hashline 모델별 표: Grok Code Fast 1 6.7%→68.3%, Grok 4 패치 실패 50.7%→대폭 감소, Gemini 3 Flash 78.3% — `appendix_b:97-102`
- (내부) 교훈 3줄 중 백미: **"도구 인터페이스 변경은 '무료 모델 업그레이드'와 같습니다."** / "'모델의 성능이 부족하다'는 진단은 종종 오판입니다" — `appendix_b:118-119`
- (내부) 토큰 마비 3단계: 컨텍스트 잠식 → 선택 혼동 → 연쇄 오류. description+when_to_use **합산 1,536자 하드 상한** — `appendix_b:138-142`
- (내부) 좋은 에러 메시지 3요소(무엇이/차이/어떻게) = **"무비용 교육"** — `appendix_b:203`
- (내부) 10항목 진단이 B.1 교차 안티패턴 4종과 7~10번으로 1:1 대응 — `appendix_b:268`

**부록 C (트러블슈팅 20선)** — 증상→원인→해결→확인 명령어 구조. 5분 셀프 진단 10항목 — `appendix_c:236-247`

**부록 D (토큰 경제와 레퍼런스 아키텍처)**
- (내부) 오프닝: 같은 규모·같은 모델인데 월 $12,000 vs $1,800 = **6.7배** 차이 — `appendix_d:24`
- (내부) 원리 1: **입력이 비용의 99%.** 입출력 비율 약 100:1 — `appendix_d:32`
- (내부) 누적 계산의 함정: 20회 루프 = 등차 누적 약 **2.25M 토큰**. "세션당 50K" 단순 가정과 **45배 차이** — `appendix_d:58-60`
- (내부) 원리 2: **KV 캐시는 10배 레버.** 3원칙 = 안정적 접두사 / 추가 전용 / 바이트 동일 접두사. Fork 서브에이전트로 80% 절감 — `appendix_d:82-94`
- (내부) 원리 3: 10인 팀 월 비용 A(없음) ~$7,000 / B(기본) ~$2,700(62%↓) / C(최적화) ~$1,230(82%↓). 연 $69,240 차이 — `appendix_d:104-110`
- (내부) 민감도: 캐시 히트율 ±15%p가 비용 +47%/-31% → **1등 레버는 캐시, 2등 모델 분리, 3등 루프 길이** — `appendix_d:120-126`
- (내부) 원리 4: Stripe Minions 6계층 vs 클로드 코드 7계층 → **공통 관심사 7가지**(물리적 격리 / 행동 범위 축소 / 빠른 피드백 / 깊은 검증 / 우아한 실패 / 하이브리드 교차 / 최후 방어선) — `appendix_d:137-178`
- (내부) 건축 비유: "30층짜리 건물을 짓는다고 하자. 건축가가 '나는 7층이 좋겠다'고 말할 수 없다" — `appendix_d:134`
- (내부) 원리 5: 확률/결정론 교대. **6개 규칙을 전부 자연어로 지시하면 준수 확률 약 37%**, 5개를 훅으로 옮기면 준수율↑ 비용↓ — `appendix_d:229-235`
- (내부) 훅의 경제성: "Hooks는 컨텍스트 비용이 전혀 발생하지 않는 결정론적 제어 메커니즘… 토큰 소모는 0이 되고 준수율은 100%로 고정" — `appendix_d:229`

---

## 근거 D — Part 간 논리적 연결고리 (요청 3)

각 연결은 책이 **명시적으로** 밝힌 것만 채택했다.

**1부 → 2부.** 1부는 "왜"를 진단하고 처방(5축: 권한·도구·검증·상태·관측)까지만 제시하고 구현은 미룬다. 1장 본문이 직접 예고: "이 다섯 축이 실제로 어떻게 구성되는지는 2부에서 따로 다룹니다"(`chapter_01:161`). 3장 도입은 이를 받아 "앞부분(1·2장)에서 '왜 하네스인가'를 이해했다면, 이제 질문은 '어떻게 구성하는가'로 옮겨갑니다"(`chapter_03:36`).

**2장 → 3장 (Part 내부의 다리).** 2장에서 만든 파일 5개를 3장이 그대로 해부한다. "이 장은 그 다섯 파일을 세 덩어리로 나눠 봅니다"(`chapter_03:34`). 즉 실습이 먼저, 개념화가 나중 — 의도적 순서.

**3장 → 4·5·6장.** 3장이 경계선만 긋고 각 기둥을 한 장씩 심화. "3장은 2부의 지도입니다"(`chapter_03:36`). 실제로 4장은 '## 팀 통신 프로토콜' 섹션을 비워둔 채 넘기고(`chapter_06:226`), 6장이 그 섹션을 채우면서 회수한다.

**2부 → 3부 (가장 중요한 연결).** 3부의 존재 이유는 **반복**이다. 7장 도입이 서사로 설명: 코드 리뷰 팀 → 풀스택 팀 → 마이그레이션 팀, "여섯 번째 팀을 만들던 날, 엔지니어는 에디터 화면에서 똑같은 작업 패턴이 반복되고 있음을 깨닫습니다"(`chapter_07:36`). 그리고 2부에서 배운 원칙("반복되는 절차가 스킬의 재료")을 2부 자신에게 적용한다 — "2부에서 본 여섯 단계 자체가 스킬이 되지 못할 이유가 없습니다"(`chapter_07:45`). **결정적 증거: "2부 전체가 메타스킬의 references/에 모듈로 접혀 있는 셈입니다"**(`chapter_07:109`).

**3부 내부 순서 (7·8 = 설계 / 9·10 = 운영).** 저자가 직접 갈라 놓음: "앞 두 장은 설계의 골격으로… 뒤 두 장은 운영의 골격으로"(`00_front_matter.md:159`). 8장은 "무슨 모양", 9장은 "그 모양을 어떻게 돌릴까"로 이어진다 — "8장이 팀의 모양을 결정했다면, 이 장은 그 팀을 어떻게 작동시킬지를 결정합니다"(`chapter_09:7`). 10장은 마지막 관문: 등록하지 않으면 첫 세션 이후 호출되지 않으므로 앞의 모든 설계가 무효화된다(`chapter_10:44-53`).

**3부 → 4부.** 8장 말미에 **명시적 예고표**가 있다(`chapter_08:423-429`):

| 패턴 | 4부 장 |
|---|---|
| 파이프라인 + 팬아웃·팬인 | 11장 코드 리뷰 |
| 계층적 위임 + 파이프라인 | 12장 풀스택 |
| 감독자(동적 분배) | 13장 레거시 마이그레이션 |
| 생성-검증 + 팬아웃 | 14장 디버깅·RCA |
| 스케일별 혼합 | 맺음말 1인→팀→조직 |

저자 표현: **"8장은 패턴의 '지도'를 살펴봤습니다. 실제 '지형'은 4부에서 보여줍니다."**(`chapter_08:421`)

**4부 내부 배치.** 4개 도메인은 난이도·규모 순이 아니라 **패턴 커버리지** 순이다. 11장(가장 익숙한 문제 = 팬아웃) → 12장(경계면 = 복합 패턴 4개) → 13장(규모 = 감독자) → 14장(사후 대응 = 결정론 게이트). 그리고 11장과 14장은 **순환 피드백**으로 닫힌다: "디버깅 팀이 찾은 근본 원인 패턴이 새로운 코드 리뷰 규칙을 만들어 내고, 코드 리뷰에서 놓친 케이스가 디버깅 팀의 입력이 됩니다"(`chapter_14:342`).

**4부 → 5부(부록).** 부록 B가 4부 전체를 횡단해 회수한다: "코드 리뷰 팀과 풀스택 팀, 레거시 마이그레이션과 RCA 팀은 서로 다른 도메인을 다루지만 **실패 모드는 놀랍도록 비슷합니다**"(`appendix_b:7`). 부록 D는 1장이 던진 "왜 산업이 같은 구조에 도달했는가"를 Stripe/클로드 코드 계층 비교로 답한다.

**전체를 닫는 고리.** 1장 파울러의 "안쪽은 확률론, 가장자리는 결정론"(`chapter_01:128`)이 부록 D 원리 5에서 워크플로 8단계 교대표로 구체화된다(`appendix_d:218-227`). 즉 1장 = 명제, 부록 D = 구현. 책이 수미상관 구조.

---

## 근거 E — 인용하기 좋은 문장 (요청 4, 원문 그대로)

**책 전체의 명제**
> "모델이 아니라 하네스가 결과를 결정합니다." — `00_front_matter.md:109`

**진단**
> "혼자 쓴 PR을 혼자 머지(merge)하는 구조입니다." — `chapter_01:50`
> "자기 출력물을 자기가 검토하는 것은, 자신이 쓴 글을 자신이 교정하는 것과 같다." — `chapter_01:50`

**잔 볼루크 (Hashline)**
> "때로는 모델이 일을 이해 못 하는 게 아닙니다. 모델이 자신을 표현하는 단계에서 흔들릴 뿐입니다. 파일럿을 탓하는 동안 착륙 장치가 고장 나 있는 겁니다." — `chapter_01:91`

**채드 파울러 (Relocating Rigor, 2026-01)**
> "소프트웨어 역사의 어떤 전환은 익숙한 통제 신호를 치워내기 때문에 자유처럼 느껴집니다. 실제로는 엄격함을 진실이 사는 곳 가까이로 옮기고 있을 뿐입니다." — `chapter_01:124`
> "패턴은 이렇습니다. 안쪽은 확률론, 가장자리는 결정론." — `chapter_01:128`
> "기계가 확인할 수 있는 것은 결정론적 도구에게, 판단이 필요한 것은 확률적 에이전트에게." — `appendix_d:216`

**OpenAI Codex 팀 (에이전트 실패의 재정의)**
> "어떤 부분이 빠져 있고, 그것을 어떻게 에이전트가 인식하고 강제할 수 있는 형태로 만들 것인가." → 저자 요약: **"에이전트가 실패하면, 모델의 한계가 아니라 환경 설계의 문제로 다룬다."** — `chapter_01:141`

**아카시 굽타 (시대 규정)**
> "2025 was agents. 2026 is agent harnesses." — `chapter_01:149`

**프롬프트 vs 하네스**
> "프롬프트는 설득에 의존하지만, 하네스는 기계적 강제에 의존합니다. 프롬프트는 말로 지시합니다. 하네스는 구조로 강제합니다." — `chapter_01:192`

**핵심 운영 원칙**
> "파일이 없으면 존재하지 않는다." — `chapter_02:84`
> "AI 결과물은 초안이다." — `chapter_02:429`
> "오케스트레이터는 지시자가 아니라 지휘자입니다." — `chapter_03:106`
> "컨텍스트는 내 것이 아니라 공공재입니다." — `chapter_05:132`
> "3명의 집중된 팀원이 5명의 산만한 팀원보다 낫습니다." — `chapter_06:444`
> "'리더를 거치지 않는다'는 설계 원칙은 성능의 문제가 아닙니다. 병렬성의 문제입니다." — `chapter_06:61`
> "QA Phase는 만들지 않는다." — `chapter_12:198`
> "기본은 팀, 경량은 서브, 현실은 하이브리드입니다." — `chapter_09:373`

**사고 기록 (가장 인용력 높은 사례)**
> "세션 9a990de8이 2분 내 292개 에이전트를 생성하여 36.8GB RSS에 도달했습니다. 이 사례 이후 팀원 한 명이 받을 수 있는 메시지 수가 최대 50개로 제한되었습니다." — `chapter_06:430`

**오케스트레이터 설계**
> "'모든 것이 성공한다'고 가정하는 오케스트레이터는 프로덕션에서 가장 먼저 무너집니다." — `chapter_06:262`

**메타스킬 / 도구 설계**
> "메타스킬은 자신이 정의한 규칙을 자기가 따릅니다." — `chapter_07:111`
> "도구 인터페이스 변경은 '무료 모델 업그레이드'와 같습니다." — `appendix_b:118`

**아키텍처 수렴**
> "30층짜리 건물을 짓는다고 하자. 건축가가 '나는 7층이 좋겠다'고 말할 수 없다. 물리 법칙, 건축 법규, 거주자의 동선, 화재 대피 경로가 층수와 각 층의 역할을 결정한다." — `appendix_d:134`

**비르기타 뵈켈러**
> "하네스 엔지니어링은 완전히 새로운 것이 아니라 기존 소프트웨어 공학 패러다임이 새롭게 진화한 결과다." — `appendix_d:207`
> "일반적인 애플리케이션 토폴로지를 위한 하네스 세트가 마치 현재의 서비스 템플릿처럼 표준화될 것이다." — `chapter_14:519`

**피터 스타인버거**
> "I ship code I don't read(저는 코드를 읽지 않고 배포합니다)." — `chapter_14:366`

**규모 전환의 철칙**
> "거버넌스 N단계에서 N+1단계의 병렬화를 시도하지 않는다." — `chapter_14:402`

**역방향 콘웨이 법칙**
> "원하는 하네스 아키텍처를 먼저 설계하고, 그에 맞춰 조직 구조를 조정한다." — `chapter_14:542`

**닫는 문장**
> "하네스 구축이란 완벽한 문서를 만드는 일이 아니라, 같은 실수를 반복하지 않는 환경을 만드는 일이라는 점입니다." — `closing_마치며.md:26`

**미첼 하시모토 (책의 마지막 인용)**
> "챗봇과 싸우지 말고, 하네스를 설계하라." — `closing_마치며.md:28`

---

## 근거 F — 외부 팩트체크 (좁게 수행)

책이 인용한 외부 사례 중 검증 가능한 것만 확인했다. **핵심 사례는 전부 실재하며 수치도 일치**한다. 단 부록 D의 가격표는 현재 기준으로 틀렸다.

### ✅ 확인됨 — 수치 일치

| 책의 주장 | 검증 결과 | 출처 |
|---|---|---|
| 리플릿/제이슨 렘킨 DB 삭제 (2025-07), 거짓 보고 포함 | 실재. 렘킨 본인 증언 "I told it 11 times in ALL CAPS DON'T DO IT". 롤백 불가라고 보고했으나 실제로는 가능했음 | [The Register](https://www.theregister.com/2025/07/21/replit_saastr_vibe_coding_incident/), [Fortune](https://fortune.com/2025/07/23/ai-coding-tool-replit-wiped-database-called-it-a-catastrophic-failure), [AI Incident DB #1152](https://incidentdatabase.ai/cite/1152/) |
| Airbnb: 3,500개 테스트 파일, 18개월 → 6주, 97% 자동화 | **정확히 일치.** 40k~100k 토큰 컨텍스트, 상태 기계, 재시도 루프도 원문 그대로 | [Airbnb Tech Blog](https://medium.com/airbnb-engineering/accelerating-large-scale-test-migration-with-llms-9565c208023b), [InfoQ](https://www.infoq.com/news/2025/03/airbnb-llm-test-migration/) |
| Vercel: 스킬 56% 미호출 / 베이스라인 53% → AGENTS.md 100% | **표 전체 일치.** 스킬 기본 53%(+0pp), 명시적 지시 79%, AGENTS.md 100% | [Vercel Blog](https://vercel.com/blog/agents-md-outperforms-skills-in-our-agent-evals) |
| Vercel: 도구 15개 → 2개, 정확도 80% → 100% | 일치. 원문은 text-to-SQL 에이전트 d0 사례. 추가로 3.5배 속도, 토큰 37% 절감, 단계 42% 감소 | [Vercel Blog](https://vercel.com/blog/we-removed-80-percent-of-our-agents-tools), [Vercel Blog](https://vercel.com/blog/testing-if-bash-is-all-you-need) |
| 랭체인 DeepAgents: Terminal Bench 2.0에서 52.8 → 66.5, Top 30 밖 → Top 5 | **정확히 일치.** 모델(gpt-5.2-codex) 고정, 시스템 프롬프트·도구·미들웨어만 변경 | [LangChain Blog](https://blog.langchain.com/improving-deep-agents-with-harness-engineering/) |
| Hashline: Grok Code Fast 1 6.7% → 68.3%, Grok 4 Fast 토큰 61% 절감 | 일치. 16개 모델 × 3개 편집 도구 벤치마크 | [Vstorm OSS](https://oss.vstorm.co/blog/hashline-file-editing/), [DEV](https://dev.to/alexchen31337/the-harness-problem-is-real-and-the-edit-tool-is-where-it-starts-nff) |
| Manus, 2025년 12월 Meta에 약 20억 달러 인수 | 일치. 2025-12-29 발표, WSJ·로이터 추정 $2~3B. 메타의 3번째로 큰 인수 | [CNBC](https://www.cnbc.com/2025/12/30/meta-acquires-singapore-ai-agent-firm-manus-china-butterfly-effect-monicai.html), [Bloomberg](https://www.bloomberg.com/news/articles/2025-12-29/meta-acquires-startup-manus-to-bolster-ai-business) |
| OpenAI: 5개월 100만 줄, 수동 코드 0줄, 1인당 하루 3.5 PR, 약 1,500 PR | **정확히 일치.** 2025년 8월 말 시작, 3인 팀, 빈 레포에서 출발 | [OpenAI: Harness engineering](https://openai.com/index/harness-engineering/), [Latent Space](https://www.latent.space/p/harness-eng) |

**writer가 쓸 만한 추가 발견:** OpenAI가 이 회고를 발표한 글의 제목이 문자 그대로 **"Harness engineering: leveraging Codex in an agent-first world"**다. 즉 이 책이 쓰는 용어는 저자의 조어가 아니라 2026년 상반기에 OpenAI·랭체인이 동시에 쓰기 시작한 업계 용어다. (랭체인 글 제목도 "Improving Deep Agents with harness engineering".) 1장의 "2025 was agents. 2026 is agent harnesses" 주장을 뒷받침하는 독립 증거.

**Hashline 저자 표기 주의:** 책은 "잔 볼루크(Can Boluk)"로 표기. 실제 표기는 **Can Bölük**(터키어 표기, GitHub `can1357`), 원 블로그 글은 "The Harness Problem"(2026년 2월). 한국어 음차는 "잔"보다 "잔 뵐뤽"에 가깝다. 인용 시 원어 병기 권장.

### ⚠️ 확인됨 — 수치 불일치 (인용 시 주의)

**부록 D의 Anthropic 가격표(`appendix_d:66-70`)는 "2026 Q2 기준"이라고 명시했으나 현재 공식 가격과 다르다.**

| 모델 | 책의 표기 (입력/캐시히트/출력) | 실제 현재 가격 | 판정 |
|---|---|---|---|
| Opus 4.6 | $15 / $1.50 / $75 | **$5 / $0.50 / $25** | ❌ 3배 과대. $15/$75는 Opus 4.1 이전 세대 가격 |
| Sonnet 4.6 | $3 / $0.30 / $15 | $3 / $0.30 / $15 | ✅ 정확 |
| Haiku 4.5 | $0.80 / $0.08 / $4 | **$1 / $0.10 / $5** | ❌ Haiku 3.5 가격과 혼동한 것으로 보임 |

출처: `claude-api` 스킬의 모델 카탈로그(캐시 2026-06-24) 및 `shared/prompt-caching.md`. 캐시 읽기는 기본 입력가의 약 0.1배, 캐시 쓰기는 5분 TTL 1.25배 / 1시간 TTL 2배.

**파급 효과 2건:**
1. 부록 D 안티패턴 ②의 **"Haiku는 Opus 대비 약 1/19"**(`appendix_d:250`)는 잘못된 두 수치의 비($15÷$0.80)에서 나온 값이다. 실제 비율은 **약 1/5**($5÷$1). 모델 분리의 절감 효과가 책의 서술보다 작다.
2. 반면 **"KV 캐시는 10배 레버"**(`appendix_d:62`)는 절대 가격과 무관한 비율(캐시 읽기 = 입력가의 0.1배)이므로 **여전히 유효**하다. 부록 D의 결론(1등 레버는 캐시)은 흔들리지 않는다.

→ writer 지침: 부록 D를 인용할 땐 **구조적 주장(입력 99%, 캐시 10배 레버, 계층 수렴)만 쓰고 절대 가격표는 인용하지 말 것.** 굳이 쓴다면 "출간 시점 기준이며 현재는 다름"을 병기.

### ⚠️ 경미한 표기 차이

- 리플릿 사건 피해 규모를 책은 "임원 레코드 1,206명분과 **작업 레코드 1,196개**"로 적었다(`chapter_01:32`). 렘킨의 원 트윗은 "1,206 executives and **1,196+ companies**" — 즉 "작업 레코드"가 아니라 **"회사 레코드"**로 보인다. 번역 과정의 오기 가능성. 인용 시 "1,206명의 임원 및 1,196개 이상의 회사 데이터"로 쓰는 편이 안전.

---

## 인사이트 후보

블로그가 "인사이트 중심"이므로, 요약이 아니라 **판단**이 될 만한 것들만 추렸다. 각 항목은 근거와 함께 제시했고, 마지막 두 개는 writer/사용자의 경험이 필요하다.

1. **이 책의 진짜 구조는 4부가 아니라 "명제 → 분해 → 접기 → 펼치기"의 4박자다.**
   1부가 명제("모델이 아니라 하네스")를 세우고, 2부가 그 명제를 3개 파일 종류로 분해하고, 3부가 그 분해 절차 자체를 메타스킬로 **접고**, 4부가 접힌 것을 4개 도메인으로 다시 **펼친다**. 결정적 근거는 `chapter_07:109`의 "2부 전체가 메타스킬의 references/에 모듈로 접혀 있는 셈입니다" — 이건 비유가 아니라 파일 구조의 사실 진술이다(SKILL.md 443라인 + references 1,706라인). 목차만 보면 안 보이는 구조.

2. **책이 자기 방법론으로 자기를 만들었다는 사실이 가장 강한 논증이다.**
   4장은 실제 집필 하네스의 8개 에이전트를 열어 보이며 "권장 설계(모델별 분리)와 현재 운영(전원 opus, tools 없음)이 다르다"고 스스로 폭로한다(`chapter_04:543-571`). 10장은 book-writer 하네스의 CLAUDE.md 변경 이력을 그대로 공개한다(`chapter_10:319`). 5장은 이 책을 집필 중인 book-writer/SKILL.md를 예제로 쓰면서 "지금 이 장을 포함한 모든 장이 이 스킬의 지휘 아래 집필됐습니다"라고 명시한다(`chapter_05:246`). 7장의 자기 참조성 논의는 이 실천의 이론화다. → **하네스 책이 신뢰할 만한지 판정하는 기준은 "저자가 그걸로 이 책을 썼는가"이고, 이 책은 그 검사를 통과한다.**

3. **"파일이 없으면 존재하지 않는다"가 이 책의 유일한 공리다.**
   2장에서 등장(`chapter_02:84`)해 4장(`chapter_04:47`), 7장 Phase 3(`chapter_07:272`)까지 세 번 반복된다. 그리고 나머지 모든 규칙이 여기서 파생된다 — 왜 역할을 파일로 쓰나(4장), 왜 스킬은 디렉터리+SKILL.md여야 하나(2장), 왜 CLAUDE.md 포인터가 필요한가(10장), 왜 `_workspace/`는 TeamDelete 후에도 남기나(6장), 왜 batches.json을 마크다운 대신 JSON으로 쓰나(13장). **"영속성이 곧 존재"라는 한 문장으로 책의 8할이 재구성된다.**

4. **저자는 "권한 = 프롬프트가 아니라 파일"이라는 원칙을 4개 실전 장에서 똑같은 모양으로 반복한다.** 이게 4부의 숨은 통일 원리다.
   - 11장: 리뷰어 3인에게 Edit 없음, refactorer만 Edit, 그것도 `.diff` 파일로만 (`chapter_11:74`)
   - 12장: verifier는 검증만, 수정 금지 — "verifier가 직접 고치면 구현 에이전트가 실수를 학습하지 않고 책임 경계가 무너진다" (`chapter_12:292`)
   - 13장: migration-validator에 Edit 없음 — "스스로 고쳐서 통과 처리하는 문제를 구조적으로 차단" (`chapter_13:283`)
   - 14장: Edit는 오직 fix-proposer — "확증 편향을 구조적으로 차단" (`chapter_14:258`)
   → 도메인은 다 다른데 **"검증자에게서 쓰기 권한을 빼앗는다"는 한 수가 네 번 반복**된다. 4장의 "tools 한 줄이 만드는 파일 수준 가드레일"(`chapter_04:238`)의 실전 회수.

5. **가장 반직관적인 챕터는 10장이고, 그 이유는 Vercel 표다.**
   책 전체가 "스킬을 잘 설계하라"고 9장까지 가르쳐 놓고, 10장에서 **스킬(기본)이 베이스라인 대비 +0pp였고 단순한 정적 마크다운이 +47pp였다**는 데이터를 들이민다(`chapter_10:67-74`). 저자는 이 모순을 "스킬은 판단이 필요한 수동 자원, CLAUDE.md는 always-on"이라는 계층 논리로 봉합한다(`chapter_10:76-78`). → **하네스 설계에서 "더 정교한 것이 더 낫다"는 직관은 자주 틀린다.** 부록 B의 "bash is all you need"(`appendix_b:146`)와 Hashline의 "도구 인터페이스 변경 = 무료 모델 업그레이드"(`appendix_b:118`)가 같은 교훈의 다른 얼굴이다.

6. **책이 제시하는 숫자 중 가장 실무에 쓰이는 건 +60%가 아니라 "3~5명"과 "50개"다.**
   +60%는 인상적이지만 재현하기 어렵고(저자 내부 실험), 조건도 까다롭다(7장이 "잘 설계된 팀"임을 강조). 반면 팀 크기 3~5명(`chapter_06:438`), 7명 넘으면 Phase 분리(`chapter_08:279`), 메시지 큐 50개 상한(`chapter_06:430`), 계층 2단계 이내(`chapter_08:298`), 재시도 2~3회(`chapter_08:168`), CLAUDE.md 150줄(`appendix_b:236`), 도구 15개(`appendix_b:145`), 캐시 히트율 70%(`appendix_d:258`) — 이 상한선들은 전부 **사고에서 역산된 값**이고 그대로 체크리스트가 된다. → **하네스 설계는 "무엇을 넣을까"가 아니라 "어디서 멈출까"의 기술에 가깝다.**

7. **부록 D가 사실상 숨은 5부이자 이 책의 이론적 종착점이다.**
   1장 파울러의 "안쪽은 확률론, 가장자리는 결정론"이 부록 D 원리 5에서 8단계 교대 워크플로로 구체화되고(`appendix_d:218-227`), 1장이 던진 "왜 산업이 같은 구조에 도달했는가"가 Stripe 6계층 vs 클로드 코드 7계층 → 공통 관심사 7가지로 답해진다(`appendix_d:170-178`). 그리고 "6개 규칙을 자연어로 지시하면 준수율 37%, 훅으로 옮기면 토큰 0 · 준수율 100%"(`appendix_d:229`)는 1장의 "프롬프트는 설득, 하네스는 강제"(`chapter_01:192`)에 **처음으로 숫자를 붙인 지점**이다. → **부록이라 부르지만 읽는 순서는 1장 직후가 나을 수 있다.**

8. **책이 스스로 유효기간을 설계해 뒀다.** — 가장 성숙한 대목
   10장의 ADR '삭제 조건' 필드와 Build to Delete 원칙(`chapter_10:400-426`), 그리고 "오늘 정교하게 설계한 하네스의 많은 구성요소가, AI 모델이 충분히 발전하면 불필요해질 것"(`chapter_10:323`). 예시 ADR의 삭제 조건이 "단일 리뷰어 모델이 두 관점을 한 번에 안정적으로 다룰 수 있는 역량에 도달했을 때"(`chapter_10:421`)라는 것도 상징적이다. → **하네스는 모델의 결함을 메우는 임시 구조물이고, 좋은 하네스는 자기가 언제 사라져야 하는지를 문서화한다.** 방법론서가 자기 소멸 조건을 명시하는 건 드물다.

9. **(writer/사용자가 채울 것) 이 책을 실제로 따라 해본 경험이 있다면 어디가 갈라졌는가.**
   특히: 2장 파일 5개를 30분 안에 만들었는가 / 6장 프리미티브(TeamCreate·SendMessage)를 실제 클로드 코드 버전에서 쓸 수 있었는가 / 10장 CLAUDE.md 포인터가 실제로 새 세션에서 트리거됐는가. 이 3개가 책의 주장 중 **가장 검증하기 쉬운 지점**이자, 안 되면 나머지가 다 무의미한 지점이다.

10. **(writer/사용자가 채울 것) 이 저장소 자체가 하나의 반례이자 사례다.**
    이 책을 챕터 단위로 전사한 작업(`output/chapters/` 21개 파일, 커밋 `88efc20`, `6b26d14`의 "16개 구간 병렬 QA")은 그 자체가 팬아웃·팬인 + 생성-검증 패턴이다. 책 없이 직관으로 그 구조에 도달했는지, 아니면 책을 읽고 적용했는지 — 어느 쪽이든 글감이 된다. 특히 `6b26d14`의 "원문 대조 검증(16개 구간 병렬 QA)"은 5장 With/Without과 12장 boundary-verifier의 실사용례로 읽힌다.

---

## 미해결 질문

> **[2026-07-29 verifier]** 아래 항목의 해소 여부는 각 항목 끝에 표시했다. 상세는 문서 맨 아래 `## 검증 기록` 참조.

**verifier가 검증해야 할 것**

1. **부록 D 가격표는 확정적으로 틀렸다** (위 근거 F). writer가 이 표를 인용하면 verifier가 반드시 잡을 것. 애초에 인용하지 않는 게 안전.
2. **"claude-code-harness" A/B 실험(49.5 → 79.3)의 공개 자료를 찾지 못했다.** 저자 내부 실험이라 재현/검증 불가. 15개 태스크의 구성, 채점 기준, 난이도 구간 정의 모두 미공개. → 인용 시 **"저자의 내부 실험"임을 반드시 명시**. 외부 검증된 Hashline·Terminal Bench와 같은 층위로 나열하면 오해를 부른다.
   → **[해소] '저자 내부 실험'은 책이 직접 명시한다.** `chapter_01:69` "저자가 실행한 내부 실험입니다. claude-code-harness라는 A/B 테스트 프로젝트에서 동일한 Sonnet 모델에 동일한 15개 실무 코딩 태스크를 두 번 풀게 했습니다." 다만 **"난이도 구간 정의 모두 미공개"는 이 노트의 과장**이었다 — 실험 설계(모델 고정·과제 수·`.claude/` 유무 두 레포)와 난이도 구간별 격차(초고난이도 +36.2점, `chapter_07:143`은 쉬운 과제 +23.8점)는 공개돼 있다. 미공개인 것은 **채점 기준과 개별 과제 목록**뿐. 초안도 이 노트를 따라 "과제 구성이 공개돼 있지 않아"라고 썼기에 교정했다. 검증 기록 3번 참조.
3. **292개 에이전트 / 36.8GB RSS / 메시지 큐 50개 상한** — "클로드 코드 내부 사고 사례"로 출처 표기(`chapter_06:430`). 공개 문서에서 확인 불가. 다만 8장에서 저자가 "제가 직접 겪은"이라고 쓴 반면(`chapter_08:271`) 6장에서는 "클로드 코드 내부 사고 사례"로 인용해 **출처 표기가 서로 어긋난다**. 인용 시 주의.
   → **[부분 해소 — 2026-07-29 2차]** 2차 보강본이 이 사건을 본문에 채택했다(1차에서는 회피). 사건 자체의 외부 검증은 **여전히 불가**(공개 문서 없음). 대신 초안이 이를 "책이 인용한 사고 기록"으로 명시하고 6장 attribution을 축자로 밝히는 형태로 처리했고, 8장과의 표기 충돌을 밟지 않도록 주어를 "책은"에서 "6장은"으로 좁혔다. **책 내부의 출처 표기 불일치 자체는 미해소.** 검증 기록 35·40번 참조.
   → **[3차 — 좁힘 유지 확인]** 3차 재작성본은 장 번호를 쓰지 않으므로 주어가 "6장은" → "오케스트레이터를 다루는 장은"으로 바뀌었다. 지시 대상은 그대로 6장이며 8장 표기와 충돌하지 않는다. **여전히 "책은"으로 되돌리면 안 됨.** 검증 기록 53번 참조.
   → **[4차 — 지칭 없는 좁힘으로 재구성, 유효]** 4차본은 장 지칭 자체를 없애고 **인용문에 붙은 attribution을 가리키는 형태**("인용 끝에는 출처가 …로 붙어 있다")로 바꿨다. 초안이 싣는 인용문은 조사 "9a990de8**이**"까지 6장 판(`chapter_06:430`)과 일치하고, 그 판에만 attribution이 붙는다(8장 판 `chapter_08:273`은 attribution 없이 재게재). 따라서 "인용 끝에는 …로 붙어 있다"는 초안이 인용한 그 게재분에 대해 참이며 8장과 충돌하지 않는다. 검증 기록 62번 참조. **책 내부의 출처 표기 불일치 자체는 여전히 미해소.**
4. **HTekDev 2026 조사(기업당 평균 12개 에이전트, 73% 모니터링 없음)** — 원 조사 미확인. 검증 안 함.
5. **11장 Without/With 수치(2/4→4/4, 8분→3분, 토큰 2.2배)** — 저자가 본문에서 "추정값"이라고 명시(`chapter_11:314`). 실측 아님. 인용한다면 이 단서를 함께.
6. **Stripe Minions "주 1,000개 PR + 300만 건 자동 테스트", "400개 내부 도구 중 15개 선별"**(`chapter_14:521`, `chapter_04:249`) — 이번 검증 범위에서 제외. 인용 전 확인 필요.
7. **스타인버거 월 6,600커밋** — 미검증. 책 자체도 "13년 동안 쌓은 부트스트랩 규율의 결과"라며 일반화를 경계(`chapter_14:573`).
8. **책이 서술하는 클로드 코드 API 표면의 현재성.** TeamCreate/TaskCreate/SendMessage/TeamDelete, 프론트매터 16개 필드, `SLASH_COMMAND_TOOL_CHAR_BUDGET`, description+when_to_use 1,536자 상한 — 전부 책 기준이며 이번에 검증하지 않았다. 클로드 코드는 빠르게 바뀌므로 **API 세부는 인용을 피하고 개념만 쓰는 편**이 안전. 부록 A도 "본문보다 공식 문서를 1순위로"라고 스스로 밝힘(`appendix_a:203`).

**writer가 `(확인 필요)`로 남기거나 회피할 것**

9. 리플릿 사건의 "작업 레코드 1,196개" → "회사 레코드 1,196개+"일 가능성 (근거 F).
10. "잔 볼루크" 표기 → Can Bölük.
11. 부록 D의 "6개 규칙 자연어 지시 시 준수율 37%" — 계산 근거(0.85^6 ≈ 0.377 추정?)가 본문에 없다. 수치 자체를 인용하기보다 "훅은 토큰 0 · 준수율 100%, 자연어는 확률적"이라는 방향만 쓰는 게 안전.

→ **[9·10·11 모두 회피로 해소]** 초안 본문(1~211행)에 셋 다 등장하지 않음을 전수 확인했다. 리플릿 레코드 건수 없음, "잔 볼루크"·"Can Bölük" 표기 없음(Hashline은 벤치마크 이름으로만 지칭), "37%" 없음. 훅 서술은 수치 대신 "컨텍스트 비용은 0이고 준수는 결정론이 된다"로 방향만 가져갔다. 검증 기록 16번 참조.

**구조적으로 답이 없는 것 (글의 논점이 될 수 있음)**

12. 책은 "모델이 아니라 하네스"라고 말하지만, 근거 F에서 확인했듯 **하네스 실험 자체가 모델 세대에 강하게 묶여 있다**(Terminal Bench는 gpt-5.2-codex 고정, Hashline은 약한 모델일수록 이득이 큼 — "the weakest models gain the most"). 모델이 강해지면 하네스의 한계 효용이 줄어드는가? 10장의 Build to Delete는 이 질문을 인정하지만 답하지는 않는다. → **블로그 글의 결론부에 놓기 좋은 열린 질문.**
   → **[해소 — 전제 사실 확인됨]** 초안이 이 논점을 결론부에 그대로 채택했으므로 전제 두 개를 재확인했다. ① 랭체인 모델 고정(gpt-5.2-codex) ② Hashline "the weakest models gain the most" — 둘 다 외부 자료로 확인. 검증 기록 12·13번 참조. **열린 질문 자체(모델이 강해질 때 하네스의 한계 효용)는 여전히 미해소이며, 초안도 "지금 시점에 답이 없는 질문"으로 명시해 다루므로 문제 없다.**

---

## 검증 기록

- 검증일: 2026-07-29
- 검증 대상: `/Users/sr/IdeaProjects/blog/_drafts/harness-engineering-book-overview.md`
- 초안에 `(확인 필요)` 플래그는 없었다. 작성자가 초안 하단 `<!-- 작성자 노트 -->`에 남긴 **인용 출처 대조표(파일:라인)를 전수 대조**하는 방식으로 검증했다.
- 1차 근거: 전사 저장소 `/Users/sr/IdeaProjects/learning/books/harness-engineering-with-claude-code/output/chapters/` (원서 황민호, 『하네스 엔지니어링 with 클로드 코드』, 한빛미디어, 초판 1쇄 2026-06-11, ISBN 979-11-7579-059-9 — 판권 확인 `00_front_matter.md:43`, `:46`, `:49`)
- 2차 근거: WebSearch / curl HTTP 상태 확인 (외부 인용 7건)
- 결과 요약: **오류 교정 7건 / 확정 20건 / 확인 불가 0건**

### A. 오류로 판정해 교정한 것 (7건)

**1. 책의 분량 표기 — "21개 챕터" → "14개 장과 부록 4편"** (초안 도입부)
- 서지/출처: 황민호, 『하네스 엔지니어링 with 클로드 코드』, 한빛미디어, 2026, 「이 책에 대하여」 — `00_front_matter.md:149` "본문은 총 4부, 14장으로 이루어져 있습니다. 추가 부록은 총 4개입니다."
- 확인 방법: 전사 저장소 파일 목록(`chapter_01`~`chapter_14` 14개 + `appendix_a`~`appendix_d` 4개) 대조 및 본문 진술 직접 인용.
- 본문 대비 정합성: **원고가 근거보다 과대.** "21"은 책의 장 수가 아니라 **전사 저장소의 파일 수**(14 + 4 + `00_front_matter` + `closing_마치며` + `index_찾아보기` = 21)다. 저장소 작업 산출물이 독자용 서술로 새어 들어간 사례. 같은 뿌리의 "뒤의 20개 챕터"도 함께 교정(아래 2번).

**2. 1장 이후 분량 — "뒤의 20개 챕터" → "뒤의 13개 장과 부록 4편"**
- 서지/출처: 위와 동일 (`00_front_matter.md:149`)
- 확인 방법: 14장 − 1장 = 13장, 부록 4편.
- 본문 대비 정합성: 1번과 동일한 계산 오류의 파생. 교정 후 초안의 "설득과 강제의 변주" 논지는 그대로 성립.

**3. claude-code-harness 실험의 비공개 범위 — "과제 구성이 공개돼 있지 않아" → "과제 목록이 공개돼 있지 않아"**
- 서지/출처: `chapter_01_왜_하네스인가.md:69` — "첫째, 저자가 실행한 내부 실험입니다. claude-code-harness라는 A/B 테스트 프로젝트에서 동일한 Sonnet 모델에 동일한 15개 실무 코딩 태스크를 두 번 풀게 했습니다. 한 번은 .claude/ 구성이 없는 레포, 다른 한 번은 에이전트 정의·스킬이 심어진 레포. 평균 점수는 49.5에서 79.3으로 올랐습니다(+60%). … 초고난이도 구간에서는 +36.2점이었습니다." 보강: `chapter_07:141-143`(15개 과제 전부 메타스킬 자동 구성, 쉬운 과제 +23.8점).
- 확인 방법: 저장소 전역 `grep -rn "claude-code-harness"` → `chapter_01:69`, `chapter_01:85`(표), `chapter_06:570`, `chapter_07:135` 4곳 전수 확인. 외부 공개 자료는 근거 F에서 이미 미발견.
- 본문 대비 정합성: **"저자의 내부 A/B다"는 근거에 정확히 부합**(`:69`가 문자 그대로 그렇게 쓴다). 반면 **"과제 구성이 공개돼 있지 않아"는 근거보다 강했다** — 모델 고정·과제 수·두 레포 조건·난이도 구간별 격차는 모두 공개돼 있다. 미공개인 것은 채점 기준과 개별 과제뿐이라 그 범위로 좁혔다. 이 노트 「미해결 질문」 2번의 서술 자체가 과장이었던 것이 원인이라, 노트에도 해소 표시를 남겼다.

**4. 명제 문장의 재등장 위치 — "서문에 두 번, 마지막 「마치며」에 다시 등장한다" → 축자 반복 2회는 책머리 두 곳, 「마치며」는 재진술**
- 서지/출처: 축자 동일 문장 "모델이 아니라 하네스가 결과를 결정합니다." — `00_front_matter.md:109`(「들어가며」, 본문 p.5), `00_front_matter.md:167`(「이 책에 대하여」, 본문 p.8). 「마치며」는 `closing_마치며.md:5` "결과를 결정하는 것은 모델 자체가 아니라 하네스였습니다."
- 확인 방법: `grep -rn "하네스가 결과를 결정"` 전역 실행 → 히트 2건(둘 다 `00_front_matter.md`). `closing_마치며.md` 전문 확인 결과 축자 반복 없음, 과거형 재진술만 존재.
- 본문 대비 정합성: **원고가 근거보다 강함.** "다시 등장한다 / 같은 문장이 잠그고 있다"는 축자 반복을 주장하는데 실제로는 재진술이다. 또 두 번의 반복 위치도 「들어가며」와 「이 책에 대하여」로 서로 다른 절이라 "서문에 두 번"은 부정확. 재진술 문장을 직접 인용해 명시하고 "같은 명제가 잠그고 있다"로 낮췄다. 초안의 수미상관 논지는 유지.

**5. 장 번호 오기 — "왜 스킬은 디렉터리와 SKILL.md여야 하는가(5장)" → (2장)**
- 서지/출처: `chapter_02_30분_퀵스타트.md:122` — "스킬은 반드시 {name}/SKILL.md 형태, 즉 디렉터리 안에 파일이 있는 구조여야 합니다. .claude/skills/commit-message.md처럼 단일 파일로 두면 클로드 코드가 스킬로 인식하지 못합니다." 보강: `chapter_02:86`.
- 확인 방법: `chapter_05_스킬_설계.md`에 대해 `grep -n "디렉터리"`, `grep -n "SKILL.md"` 전수 실행. 5장의 디렉터리 언급은 전부 `references/`·`workflows/` 하위 구조에 관한 것이고(`:309`, `:440`), **디렉터리+SKILL.md라는 형식 요건을 규정하는 문장은 5장에 없다.**
- 본문 대비 정합성: 명백한 장 번호 오기. 이 노트 「인사이트 후보」 3번은 같은 항목을 정확히 "(2장)"으로 적어 두었으므로, 초안 작성 단계에서 어긋난 것.

**6. 부록 D 단가표에 대한 단서 — "출간 시점 기준이라 지금과 다르므로" → "현재 공식 단가와 맞지 않으므로"**
- 서지/출처: 이 노트 근거 F 「⚠️ 확인됨 — 수치 불일치」(`appendix_d:66-70`). 책은 표를 "2026 Q2 기준"이라 명시했으나 Opus 4.6을 $15/$75로 적었고 이는 이전 세대 단가다.
- 확인 방법: 근거 F가 `claude-api` 스킬 모델 카탈로그(캐시 2026-06-24)와 대조해 이미 확정한 사항을 재사용. 초안에는 **가격 숫자가 하나도 인용되지 않았으므로** 표현만 점검했다.
- 본문 대비 정합성: 원고 표현이 "출간 시점에는 맞았다"는 함의를 준다는 점에서만 부정확. 실무 조언("절대 금액이 아니라 구조적 순위만") 자체는 근거와 일치하므로 함의만 제거했다.

**7. 부록 D 계층 수렴 논의의 위치와 질문의 출처 — "부록 D의 마지막 절이 1장이 던져놓고 답하지 않은 질문에 답한다" → "부록 D의 원리 4가, 부록 자신이 도입부에서 던져놓고 미뤄 둔 질문에 답한다"** (오류 2개 동시 교정)
- 서지/출처: `appendix_d_토큰경제_아키텍처.md` 절 구조 — 원리 1(`:30`) → 원리 2(`:62`) → 원리 3(`:100`) → **원리 4 왜 산업이 같은 구조에 도달했는가(`:130`)** → 원리 5 확률과 결정론의 교대(`:209`) → 안티패턴 4종(`:237`). 질문 제기 지점은 `appendix_d:28` — "왜 어떤 팀은 같은 도구로 같은 일을 하면서 6~8배 낮은 비용을 유지할까요? … Stripe의 6계층, 클로드 코드의 7층 … 서로 다른 팀이 비슷한 구조를 선택했다는 사실 자체가 중요한 단서입니다."
- 확인 방법: ① `grep -n "^### 원리\|^#"`로 부록 D 절 목차 추출(총 274행, 원리 4 뒤에 두 절 존재 확인). ② `chapter_01`에 대해 `grep -n "Stripe|스트라이프|계층|같은 구조|수렴"` 및 `"독립적으로|6계층|7계층|Minions|아키텍처|레이어|층"` 2회 실행 → **Stripe·6계층·7계층·구조 수렴 언급 0건.** ③ 전역 `grep -rn "6계층|7계층|Minions"` → 최초 등장은 `appendix_d:28`, 그 외 `chapter_14:521`, `chapter_10:78`, 목차. 1장 없음.
- 본문 대비 정합성: **두 군데가 근거와 어긋남.** (ⓐ) 계층 수렴은 부록 D의 마지막 절이 아니라 다섯 원리 중 네 번째다. (ⓑ) 이 질문을 던지는 것은 1장이 아니라 부록 D 자신의 도입부다 — 1장은 채드 파울러의 확률론/결정론 명제(`chapter_01:128`)를 던질 뿐 계층 수렴은 거론하지 않는다. 이 노트 근거 D의 "부록 D는 **1장이 던진** '왜 산업이 같은 구조에 도달했는가'를 …답한다"와 인사이트 7번의 같은 서술이 근거 없는 연결이었고, 초안이 이를 그대로 승계했다. **→ 근거 D·인사이트 7의 해당 문장은 오기이니 이후 재사용 금지.** 초안의 "부록 D가 이론적 종착점"이라는 판단 자체는 필자 해석이므로 유지했다.

### B. 확정한 것 — 근거와 일치해 그대로 둔 것 (20건)

**내부 근거(원서 전사본) 대조**

| # | 초안의 진술 | 서지/출처 | 확인 방법 | 정합성 |
|---|---|---|---|---|
| 8 | 책 제목이 목차상 "5부" 구성이라는 글 전체의 전제(제목·"목차에 적힌 5개 파트") | `00_front_matter.md:182`(Part 01), `:214`(Part 02), `:327`(Part 03), `:445`(Part 04), **`:532`(Part 05 부록)** | 목차 영역의 Part 표기를 grep으로 전수 추출 | **정확.** 「이 책의 구성」 산문(`:149`)은 "4부 + 부록 4개"라 적지만 **목차는 문자 그대로 Part 05까지 표기**한다. 글의 제목 명제("목차는 5부인데 구조는 4박자")가 성립하는 근거 |
| 9 | 하네스 정의·5축(권한·도구·검증·상태·관측)·범위 포함관계·다루지 않는 것(파인튜닝/RLHF/모델 아키텍처) | `chapter_01:151`, `:155`, `:170`, `00_front_matter.md:146` | 네 진술 각각 원문 대조 | 정확. 인용문 "AI 에이전트가 일하는 환경 전체를 설계하고 운용하는 구조적 체계" 축자 일치 |
| 10 | 1장이 5축 구현을 2부로 미룬다는 예고 인용 | `chapter_01:161` "이 다섯 축이 실제로 어떻게 구성되는지는 2부에서 따로 다룹니다." | 축자 대조 | 축자 일치 |
| 11 | "혼자 쓴 PR을 혼자 머지하는 구조입니다." | `chapter_01:50` | 축자 대조 | **전사본 OCR 오탈자('흔자')를 정정한 인용이 맞다.** 원문은 "흔자 쓴 PR을 흔자 머지(merge)하는 구조입니다" — 작성자 노트의 정정 처리가 타당. `(merge)` 병기 생략은 의미 영향 없음 |
| 12 | "프롬프트는 설득에 의존하지만, 하네스는 기계적 강제에 의존합니다…" / "패턴은 이렇습니다. 안쪽은 확률론, 가장자리는 결정론." / "2부 전체가 메타스킬의 references/에 모듈로 접혀 있는 셈입니다." / "메타스킬은 자신이 정의한 규칙을 자기가 따릅니다." / "8장은 패턴의 '지도'…실제 '지형'은 4부에서" / "3명의 집중된 팀원이…" / "오늘 정교하게 설계한 하네스의 많은 구성요소가…" / "30층짜리 건물을 짓는다고 하자…" / 부록 B 첫 문장 / 닫는 문장 | 순서대로 `chapter_01:192`, `chapter_01:128`, `chapter_07:109`, `chapter_07:111`, `chapter_08:421`, `chapter_06:444`, `chapter_10:323`, `appendix_d:134`, `appendix_b:7`, `closing_마치며.md:26` | 인용부호로 묶인 전 문장을 원문 행과 1:1 축자 대조 | **10건 전부 축자 일치.** 블록인용은 모두 원문 그대로 |
| 13 | 메타스킬 파일 크기 — SKILL.md 443라인, 500라인 상한 이내, references 6개 1,706라인, 본문의 약 4배 | `chapter_07:109` | 수치 4개 개별 대조 | 정확. "비유가 아니라 사실 진술"이라는 초안의 해석도 원문 구조와 부합 |
| 14 | Vercel 평가표(베이스라인 53% / 스킬 53%(+0pp) / 스킬+명시적 지시 79%(+26pp) / AGENTS.md 100%(+47pp))와 결론 인용 | `chapter_10:67-74` | 표 4행 수치 전수 대조 + 결론 문장 축자 대조 | 정확. 초안은 최종 Pass 열만 발췌(원표는 Build/Lint/Test 열도 있음) — 발췌 사실이 왜곡을 만들지 않음 |
| 15 | CLAUDE.md가 7계층 중 제0계층이자 유일한 always-on | `chapter_10:78` | 축자 대조 | 정확 |
| 16 | **의도적 배제 항목이 실제로 본문에 없음** — 부록 D 절대 가격표·Haiku/Opus 배수, 292개 에이전트 사건, 11장 Without/With 개별 수치, 준수율 37%, 리플릿 레코드 건수, "잔 볼루크" 표기 | 작성자 노트 `의도적 회피:` 줄 | 초안 전체에 대해 `292 / 36.8 / 1,206 / 1,196 / 37% / 6.7배 / 1/19 / $ / 2.2배 / 1.8배 / 8분 / 3분 / +60 / 917 / 52.8 / 66.5 / 68.3` 문자열 카운트 + 본문 전 숫자 토큰 추출·1건씩 귀속 확인 | **전부 회피 확인.** `292`·`37%`는 작성자 노트 블록(212행 이후)에만 존재하고 발행 본문(1~211행)에는 없음. `60%`는 Hashline 각주, `80%`는 Vercel 도구 사례와 각주 URL로 각각 정당한 출처 보유. **부록 D 가격 관련 숫자·배수는 0건** |
| 17 | 11장이 자기 비교표 수치를 "추정값"이라 **두 번** 명시 | `chapter_11:314` "지금부터 살펴볼 결과에서의 수치는 **추정값**이며…", `chapter_11:352` "참고로 수치는 추정값으로, 모델·PR 크기·토큰 가격에 따라 달라질 수 있습니다." | 두 행 실물 확인 | **정확히 두 번.** 초안은 개별 수치를 인용하지 않고 "자기 표의 신뢰 등급을 스스로 낮춰 적는다"는 메타 사실만 사용 — 과장 없음 |
| 18 | 4부 4개 장의 "검증자에게서 쓰기 권한 회수" 반복 (11·12·13·14장) | `chapter_11:74`, `chapter_12:292`, `chapter_13:283`, `chapter_14:258` | 4개 장 해당 행 개별 확인 | 정확. 13장 "스스로 고쳐서 통과 처리"는 원문도 따옴표로 묶은 표현이라 인용 형태까지 일치. 11장 원문은 "Edit·Write 권한을 주지 않았습니다"인데 초안은 "Edit 권한 없음"으로 축약 — 논지에 영향 없음. 11·14장 인용부호 문구는 **의미 정합한 압축이지 축자 인용은 아님**(원문: "이 에이전트들이 코드를 직접 고치면 검증 과정에서 발생한 거짓 양성(False Positive)이 그대로 커밋됩니다") |
| 19 | 14장 결정론 게이트 — 3회 연속 실행 전부 실패해야 재현 성공, exit 코드로 귀결 | `chapter_14:120-122`, `chapter_14:165` | 축자 대조 | 정확. 원문 "3회 연속 실행해 모두 `exit 1`이어야 재현 성공 선언"과 초안의 "세 번 연속 실행해 모두 실패해야"가 동치 |
| 20 | "12·13장" JSON 병기와 인용문 | 인용문 `chapter_13:120` "JSON을 택한 이유는 실수로 변경될 가능성이 적기 때문입니다. 예컨대 이 내용을 마크다운 문서에 체크리스트 형태로 기록해 뒀는데, 세션이 길어진다면 AI 모델이 체크 항목들을 다 '완료'했다고 판단해 파일을 멋대로 지우는 일이 생기기도 합니다." / 12장 측 `chapter_12:115` "JSON 항목은 AI 모델이 부적절하게 수정하거나 삭제할 가능성이 낮기 때문입니다." | 두 장 원문 대조 | **작성자의 자기 정정이 옳다.** 인용문은 13장(batches.json) 문장이 맞고, 12장도 별도로(요구사항 JSON) 같은 논거를 편다. 두 장 병기는 근거에 부합. 12장 쪽 대상은 '작업 목록'이 아니라 '요구사항'이라 총칭이 다소 느슨하나, 12장의 요구사항 항목도 `passes: false` 플래그를 단 항목 목록이라 사실 왜곡은 아님 |
| 21 | 상한선 값들 — 팀 3~5명/7명 초과 시 Phase 분리, 계층 2단계, 재시도 2~3회, SKILL.md 500라인, CLAUDE.md 150줄 | `chapter_08:277-279`, `chapter_08:300`, `chapter_08:168`, `chapter_05:386`, `appendix_b:236` | 5개 값 개별 대조 | 정확. **초안의 "전부 사고에서 역산됐다"는 해석도 원문이 뒷받침한다** — `chapter_08:275` "이 사건 이후 다음 규칙을 세웠습니다". 다만 원문은 7명 항목을 "Phase 분리 **검토** — …공식 기준은 아닙니다"로 한정하는데 초안은 "페이즈를 나눈다"로 단정. 체크리스트 성격을 말하는 맥락이라 교정하지 않음 |
| 22 | 부록 D 구조적 주장 — 입력이 비용의 대부분, 레버 순위 캐시→모델 분리→루프 길이, KV 캐시 3원칙, 훅은 컨텍스트 비용 0·준수는 결정론 | `appendix_d:26`·`:32`(99%), `:126`(순위), `:84-86`(안정적 접두사/추가 전용/바이트 동일 접두사), `:229` | 4개 항목 개별 대조 | 정확하며 **원문보다 보수적**. 초안은 "99%"를 "대부분"으로, "준수율 100%"를 "준수는 결정론이 된다"로 낮춰 적었다 — 과장 방향의 위험 없음 |
| 23 | 3부 내부 설계/운영 분리, 8→9장 연결, 8장 예고표 4행, 10장 등록 실패 시 무효화, 10장 HTML 주석 함정, ADR 삭제 조건 예시, 부록 B 교차 안티패턴 4종·"무료 모델 업그레이드"·"모델 성능 부족 진단은 오판", 7장 여섯 번째 팀 서사, 3장 "2부의 지도"/"다섯 파일을 세 덩어리로", 4장 역할 계약서·7섹션, 6장 팀 통신 프로토콜 회수·`_workspace/` 존치, 2장 파일 5개·`.bak` 실습, 오케스트레이터 "지시자가 아니라 지휘자" | `00_front_matter.md:159`, `chapter_09:7`, `chapter_08:425-428`, `chapter_10:44-53`, `chapter_10:446-448`, `chapter_10:421`, `appendix_b:32-89`·`:118`·`:119`, `chapter_07:34-36`·`:45`, `chapter_03:34`·`:36`, `chapter_04:52`·`:58`·`:252`, `chapter_06:226`·`:473`, `chapter_02:122`·`:455-461`, `chapter_03:106` | 작성자 노트 대조표의 나머지 항목을 행 단위로 전수 확인 | 전부 일치. 부수 사항 2건만 기록: ① 6장 원문은 "**4·5장에서** … 비워 두었습니다"라 하는데 초안은 "4장은"만 언급 — 4장이 비워 두는 것은 사실이라 오류 아님. ② 2장 실습은 `mv reviewer.md reviewer.md.bak`(확장자 치환이 아니라 `.bak` 덧붙임) — 초안의 "확장자를 `.bak`으로 바꿔"는 실효 동일 |
| 24 | 서지 각주 `[^book]` — 황민호, 『하네스 엔지니어링 with 클로드 코드』, 한빛미디어, 2026 | `00_front_matter.md:43`(초판 1쇄 2026년 6월 11일), `:46`(지은이 황민호), `:47`(펴낸곳 한빛미디어), `:49`(ISBN 979-11-7579-059-9) | 판권면 4개 항목 대조 | 정확 |

**외부 인용 대조 (본문의 외부 근거 전수)**

| # | 초안의 진술 / 각주 | 서지·URL | 확인 방법 | 정합성 |
|---|---|---|---|---|
| 25 | `[^openai]` OpenAI, "Harness engineering: leveraging Codex in an agent-first world" — 초안 본문의 "OpenAI가 코덱스 도입 회고를 발표하며 쓴 글의 제목이 문자 그대로 Harness engineering" | https://openai.com/index/harness-engineering/ | WebSearch로 제목 확인 → "Harness engineering: leveraging Codex in an agent-first world \| OpenAI" 반환. curl은 403(봇 차단)이나 검색 인덱스에 원문 존재 | **제목 축자 일치.** 본문 주장("저자의 조어가 아니다") 성립 |
| 26 | `[^langchain]` LangChain, "Improving Deep Agents with harness engineering" — 본문의 "랭체인도 같은 시기에 같은 표현을 썼다", "모델을 고정한 채 미들웨어·프롬프트 구조만 바꿔 순위를 끌어올린" | https://blog.langchain.com/improving-deep-agents-with-harness-engineering/ (현행 정본 https://www.langchain.com/blog/improving-deep-agents-with-harness-engineering) | curl -L: **두 URL 모두 200**. WebSearch로 제목 및 "모델을 gpt-5.2-codex로 고정, 시스템 프롬프트·도구·미들웨어만 변경, Terminal Bench 2.0" 확인 | 제목 축자 일치, 모델 고정 사실 확인. **초안이 쓴 blog.langchain.com URL은 현재도 200이라 교체 불필요** |
| 27 | `[^hashline]` "16개 모델 × 3개 편집 도구를 비교한 파일 편집 인터페이스 벤치마크. 포맷 변경만으로 일부 모델의 편집 성공률이 한 자릿수에서 60%대로 올라갔다." | https://oss.vstorm.co/blog/hashline-file-editing/ (curl 200). 원 출처는 Can Bölük, "I Improved 15 LLMs at Coding in One Afternoon. Only the Harness Changed.", blog.can.ac, 2026-02-12 | WebSearch로 벤치마크 규모("sixteen models, three edit tools")와 Grok Code Fast 1 6.7%→68.3% 확인 | **정확.** "한 자릿수 → 60%대"가 6.7%→68.3%와 부합. 초안은 저자명을 표기하지 않아 근거 F가 경고한 "잔 볼루크/Can Bölük" 음차 문제를 원천 회피 |
| 28 | 본문 결론부 "편집 도구 벤치마크에서 가장 극적인 개선을 보인 건 가장 약한 모델이었다" — 글의 열린 질문을 떠받치는 전제 | 위 Hashline 벤치마크 | WebSearch 결과에 원문 문구 "the weakest models gain the most" 확인 | **확정.** 초안의 결론부 논증("약한 모델일수록 이득이 크다면 모델이 강해질수록 하네스의 한계 효용은 줄어든다")의 사실 전제가 외부 근거로 뒷받침됨 |
| 29 | `[^airbnb]` Airbnb Engineering, "Accelerating Large-Scale Test Migration with LLMs" — 본문의 "리액트 테스트 파일 약 3,500개를 6주 만에 옮기고 97%를 자동 처리" | https://medium.com/airbnb-engineering/accelerating-large-scale-test-migration-with-llms-9565c208023b | 책 측 `chapter_13:27` 대조 + 근거 F의 외부 확인(Airbnb Tech Blog, InfoQ 교차) 재사용. curl 403은 Medium 봇 차단 | 정확. 3,500 / 18개월→6주 / 97% 세 수치 모두 책·외부 일치 |
| 30 | `[^vercel]` Vercel, "AGENTS.md outperforms Skills in our agent evals" | https://vercel.com/blog/agents-md-outperforms-skills-in-our-agent-evals (curl 200) | URL 생존 확인 + 근거 F의 표 전체 일치 판정 재사용 | 정확 |
| 31 | `[^vercel-tools]` Vercel, "We removed 80% of our agent's tools" — 본문의 "도구 15개를 2개로 줄여 정확도를 80%에서 100%로" | https://vercel.com/blog/we-removed-80-percent-of-our-agents-tools (curl 200) | URL 생존 확인 + `chapter_04:249` 대조 + 근거 F 재사용 | 정확 |
| 32 | "2025 was agents. 2026 is agent harnesses" (1장 인용) | 아카시 굽타(Aakash Gupta) 발언, 책 경유 인용 — `chapter_01:149` | 책 원문 대조 | 책 인용으로서 정확. 초안은 이를 저자 전망이 아니라 업계 용어의 방증으로 배치했고, 25·26번(OpenAI·랭체인 제목)이 그 배치를 뒷받침 |

### C. 확인 불가 (0건)

없음. 본문의 외부 인용 7건과 내부 인용 전량이 근거로 귀결됐다.

### D. 발행 전 참고

- 교정 7건에는 근거 주석 `<!-- 검증: … -->`을 초안에 남겼다. publisher가 발행 시 제거하므로 독자에게 노출되지 않는다.
- **이 노트 자체의 오류 2건을 위에서 정정했다** — 근거 D·인사이트 7의 "부록 D는 1장이 던진 질문에 답한다"(검증 기록 7번), 미해결 질문 2번의 "난이도 구간 정의 모두 미공개"(검증 기록 3번). 이 노트를 재사용할 때 해당 문장을 그대로 인용하지 말 것.

---

## 검증 기록 — 2차 라운드 (보강분)

- 검증일: 2026-07-29 (같은 날 2차)
- 검증 대상: 같은 초안. 발행본(`_posts/2026-07-29-harness-engineering-book-overview.md`)을 writer가 노트의 미사용 소재로 보강한 판. **1차에서 확정한 부분은 재검증하지 않고, `diff _posts/… _drafts/…`로 추출한 신규·수정 서술에만 집중**했다. 다만 문맥 변화로 기존 서술의 귀속이 어긋난 곳은 함께 점검했다(48번).
- 초안의 `(확인 필요)` 플래그: **1건** (OpenAI 3인→7인 / 1인당 처리량 증가). → 확정 처리(41번).
- 1차 근거: 전사 저장소 `/Users/sr/IdeaProjects/learning/books/harness-engineering-with-claude-code/output/chapters/`
- 2차 근거: WebSearch / WebFetch (OpenAI 회고 원문의 축자 인용 확보)
- 결과 요약: **오류 교정 6건 / 확정 10건(항목 묶음) / 확인 불가 0건**

### E. 2차 라운드에서 오류로 판정해 교정한 것 (6건)

**33. 5장 description 3단계 공식 표 — 셀 안의 낱말 누락 복원**
- 서지/출처: 황민호, 『하네스 엔지니어링 with 클로드 코드』, 한빛미디어, 2026, 5장 — `chapter_05_스킬_설계.md:74-78` 표. 원문 '목적' 열: ① "스킬이 다루는 동작 범위를 **클로드가** 찾기 쉽게 만든다" ② "호출 여부를 판단할 기준을 **클로드에게** 제공한다" ③ "이 스킬과 다른 스킬의 역할을 구분한다"
- 확인 방법: `sed -n '70,95p' chapter_05_스킬_설계.md`로 표 3행을 셀 단위 대조.
- 본문 대비 정합성: 초안이 1·2행에서 "클로드가"/"클로드에게"를 떨어뜨려, 목적어가 사라진 자리를 독자가 "사람이 찾기 쉽게"로 읽을 여지가 생겼다. 이 장의 논지가 "판단 주체는 클로드"라 낱말 하나가 논지에 걸린다. 원문대로 복원. 3열짜리 원표에서 '핵심 질문' 열을 뺀 것은 문맥상 드러나는 발췌라 유지.

**34. 5장 경계 조건 논거 — 원문의 유보를 단정으로 바꾼 것 복원**
- 서지/출처: `chapter_05_스킬_설계.md:90` — "경계가 없으면 여러 스킬이 같은 요청을 두고 겹칩니다. 그러면 클로드는 어느 쪽도 확신하지 못하고, 결과적으로 아무 스킬도 호출하지 **않을 수 있습니다.**"
- 확인 방법: 해당 행 축자 대조.
- 본문 대비 정합성: **원고가 근거보다 강함.** 초안은 "결국 아무 스킬도 호출하지 않는다"로 단정했다. 확률적 시스템의 실패 양상을 필연으로 서술하면 이 글이 다른 데서 지키는 기준(추정과 사실의 구분)과 어긋난다. 유보 복원.

**35. 292개 에이전트 인용의 출처 표기 — 주어를 "책"에서 "6장"으로 좁힘**
- 서지/출처: `chapter_06_오케스트레이터.md:430` 인용 끝 attribution — "— 클로드 코드 내부 사고 사례, 오케스트레이션 패턴 가이드". 충돌 지점: `chapter_08_아키텍처_패턴.md:271` — "제가 직접 겪은 다음 예시를 보겠습니다."(같은 인용을 attribution 없이 재게재)
- 확인 방법: 두 장의 해당 행을 각각 실물 확인. 8장 쪽 인용문은 "세션 9a990de8**가**", 6장 쪽은 "…9a990de8**이**"로 조사까지 다르게 전사돼 있어 별개 게재임도 확인.
- 본문 대비 정합성: **원고가 근거보다 강함.** 초안은 "책은 이 인용의 출처를 …로 표기한다. 저자가 돌린 실험이 아니라 인용해 온 사고 기록이다"라고 적었는데, "책은"이라는 주어는 8장 표기(저자 직접 경험)와 정면으로 충돌한다. 사용자 지시로 이 불일치 자체는 글에서 다루지 않기로 했으므로, 불일치를 서술하지 않으면서도 거짓이 되지 않도록 **주어를 6장으로 좁히고 추론을 "이 표기대로라면"으로 한정**했다. 미해결 질문 3번에 부분 해소 표시.

**36. OpenAI 하네스 구성 요약 — "여러 층의 검증 구조" → "여러 층으로 쌓인 구조"**
- 서지/출처: `chapter_14_디버깅_RCA_팀.md:374` — "반면 OpenAI의 하네스는 AGENTS.md, 서브에이전트 팀, 골든 프린시플 GC 에이전트, 레이어드 의존성 모델까지 여러 층으로 쌓인 거대한 구조입니다."
- 확인 방법: 해당 행 축자 대조 후 열거된 4항목의 성격 대조.
- 본문 대비 정합성: 초안은 열거 4항목 중 뒤의 둘을 "여러 층의 검증 구조"로 뭉갰다. '골든 프린시플 GC 에이전트'는 거버넌스, '레이어드 의존성 모델'은 아키텍처 제약이지 검증 장치가 아니다. 하필 이 절의 논지가 "하네스의 형태 차이"라 구성요소의 성격을 바꾸면 대비가 왜곡된다. 원문의 총칭("여러 층으로 쌓인 구조")으로 되돌림.

**37. 12장 7패턴이 7버그에서 왔다는 서술 — "그대로" → "거의 그대로"**
- 서지/출처: 버그 표 `chapter_12_풀스택_구현_팀.md:40-48`(7행), 경계면 검증 패턴 표 `chapter_12:223-231`(7행).
- 확인 방법: 두 표를 행 단위로 1:1 매핑. 버그1(projects?.filter)→패턴1, 버그2(대시보드 링크 404)→패턴3, 버그3(thumbnailUrl vs thumbnail_url)→패턴2, 버그4(대응 훅 없음)→패턴5, 버그5(template_approved 전이 누락)→패턴4, 버그6(즉시 응답에서 백그라운드 결과 접근)→패턴6, 버그7(/projects/ → /dashboard/projects/)→**패턴3 중복**. 패턴7(옵셔널 필드 처리)에는 **대응 버그 없음**.
- 본문 대비 정합성: 7개 중 6개만 버그에서 유래하고 1개는 표에 없는 항목이라 "그대로 승격"은 검증하면 어긋난다. "거의 그대로"로 낮춤. 초안의 논지("버그 목록이 판정 기준이 됐다")는 그대로 성립.

**38. 10장 book-writer 변경 이력 — "표로 보여준다" → "서술로 밝힌다"**
- 서지/출처: `chapter_10_등록과_진화.md:319` — "이 책의 집필 프로젝트 자체가 증거입니다. book-writer 하네스의 CLAUDE.md 변경 이력은 "초기 구성 — 8 에이전트 + 3-모드 스킬"에서 시작해 "book-editor 에이전트 추가(9번째)", "샘플링 재검증 워크플로 추가(4번째 모드)", "book-editor 고도화(8축 검출 규칙)", "샘플링 MUST 10건 반영" 순으로 쌓았습니다. 각 행이 직접 실행에서 발견된 결함을 교정한 이력입니다." 10장이 실제로 인쇄한 변경 이력 **표**는 `chapter_10:299-305`의 일반 예시(2026-04-05 초기 구성 / 04-07 QA 에이전트 추가 / 04-10 톤 가이드 추가)다.
- 확인 방법: `grep -n "book-writer\|book-editor\|샘플링" chapter_10_등록과_진화.md` → 히트 2건(`:34`, `:319`)뿐. `:319`가 유일한 book-writer 이력 언급이며 표가 아닌 산문. 표 블록은 `sed -n '290,308p'`로 실물 확인.
- 본문 대비 정합성: **원고가 근거보다 강함.** 인용된 다섯 항목은 축자 일치하지만, 책이 자기 이력을 "표로 그대로 공개했다"는 것은 사실이 아니다. 하필 이 절의 논지가 "책이 자기 하네스를 열어 보인다"라 공개의 형식을 부풀리면 논지 자체가 과장된다. "함께 밝힌다 / 쌓았다고 적는다"로 낮춤.

### F. 2차 라운드에서 확정한 것 (10건 묶음)

**39. 5장 신규 서술 일괄** — 스킬 미호출 56%(`chapter_05:27`), 클로드가 본문이 아니라 name·description으로 판단(`:29`), 세션 오픈 시 스킬 목록에 전체 컨텍스트 윈도의 약 1%(`:33`), 경계 조건에 "적합하지 않은 경우"를 함께 적는다(`:90`), Progressive Disclosure 3층과 각 층 질문(`:146-148`), 인용 "컨텍스트는 내 것이 아니라 공공재입니다."(`:132`), 스킬 30개 × description 500자 = 매 요청 15,000자(`:132`).
- 확인 방법: 각 행을 `sed`로 실물 출력해 대조. 3층의 층↔파일 대응(Layer 1=프론트매터 / Layer 2=SKILL.md / Layer 3=workflows·references)은 `:146-148`이 질문만 적고 파일명을 적지 않으므로 `grep -n "Layer 1\|Layer 2\|Layer 3\|프론트매터"`로 `:385-387`·`:460`을 찾아 확정("**Layer 1(프론트매터)**… **Layer 2(SKILL.md)**… **Layer 3(workflows, references)**").
- 본문 대비 정합성: 정확. 인용문 1건 축자 일치. Layer 3 질문은 원문 "무엇을 **추가로** 참고하는가"를 초안이 "무엇을 더 참고하는가"로 적었으나 괄호 안 요약이고 따옴표를 쓰지 않아 인용 왜곡 아님. 56%에는 책이 원인 미규명 단서를 달지만(`:27`) 초안은 책의 결론(`:29`)만 옮겨 과장 없음.

**40. 6장 신규 서술 일괄** — star topology → full mesh, N×(N-1)/2(`chapter_06:207`), 4명 6개 / 10명 45개와 메시지가 컨텍스트를 잠식(`:425`), 292개 에이전트 인용 축자(`:430`), 메시지 큐 50개가 사건 이후 그어진 선(`:432`).
- 확인 방법: 각 행 축자 대조. 인용문은 초안↔`:430` 전문 문자 단위 일치 확인.
- 본문 대비 정합성: 정확. **단 292 사건 자체는 공개 문서가 없어 외부 검증 불가**이며, 초안이 이를 "책이 인용해 온 사고 기록"으로 명시하는 형태(35번 참조)라 사실 주장으로 승격되지 않는다. 50개 상한의 방향("설계값이 아니라 사고 후에 그어진 선")은 `:432` "클로드 코드가 팀원 메시지 큐를 50개로 제한한 이유가 여기 있습니다"와 `chapter_08:275` "이 사건 이후 다음 규칙을 세웠습니다"가 뒷받침.

**41. ★ OpenAI 팀 3인 → 7인 확대와 1인당 처리량 증가 (초안의 유일한 `(확인 필요)` 플래그 — 확정)**
- 서지/출처: ① 책: 황민호, 앞의 책, `chapter_14_디버깅_RCA_팀.md:370` — "흥미로운 점은 팀 규모가 초기 3인에서 7인으로 늘어났는데도 1인당 처리량이 오히려 증가했다는 사실입니다. 일반적으로 인원이 늘수록 생산성이 떨어진다는 Brooks 법칙을 정면으로 깨버린 셈입니다. 핵심은 하네스 아키텍처가 온보딩 비용을 흡수했다는 데 있습니다." ② 외부 원문: OpenAI, "Harness engineering: leveraging Codex in an agent-first world", https://openai.com/index/harness-engineering/ — "roughly 1,500 pull requests have been opened and merged with a small team of just three engineers driving Codex. This translates to an average throughput of 3.5 PRs per engineer per day, and surprisingly the throughput has increased as the team has grown to now seven engineers."
- 확인 방법: `openai.com`은 봇 차단(WebFetch 403 — 1차 라운드의 curl 403과 동일)이라 **원문을 축자 인용한 2차 출처 2곳으로 교차 확인**했다. ⓐ https://zby.github.io/commonplace/sources/harness-engineering-leveraging-codex-agent-first-world/ ("starting with three engineers, growing to seven", "averaging 3.5 PRs per engineer per day", "Throughput increased as the team grew") ⓑ https://raw.githubusercontent.com/celesteanders/harness/main/docs/research/260211_openai_harness_engineering_codex.md (위 영문 문장 전문). WebSearch 결과 스니펫도 같은 문장을 반환. 추가로 Latent Space 인터뷰(https://www.latent.space/p/harness-eng)에서 "You kicked off as a team of three people… 1500 prs"와 7인 규모 언급을 확인.
- 본문 대비 정합성: **확정.** "the throughput"은 바로 앞 문장의 "average throughput of 3.5 PRs **per engineer** per day"를 받으므로 1인당 처리량이 맞고, "surprisingly"라는 원문의 어조까지 초안의 "오히려 증가"와 부합한다. **단 갈라지는 지점 하나** — "하네스 아키텍처가 온보딩 비용을 흡수했다"는 인과 설명은 OpenAI 회고에 **없다**(2차 출처 두 곳 모두 "onboarding 언급 없음"으로 확인). 이는 책이 붙인 해석이며, 초안이 이미 "책이 덧붙이는 관찰 … 원인은 "…"는 것이다"로 책에 귀속하고 그 뒤 "조직의 온보딩 비용을 옮겨 놓는 구조물"부터를 필자 해석으로 분리해 둔 구조라 교정 불필요. 초안에 이 갈래를 근거 주석으로 명시했다.
- 부기: "인원이 늘수록 1인당 생산성이 떨어진다"는 브룩스 법칙 서술은 책의 표현(`:370`)을 따른 것이다. 브룩스의 원 명제는 "지연된 프로젝트에 인력을 더하면 더 늦어진다"이고 1인당 생산성 하락은 그 메커니즘(통신 오버헤드)이라, 통용되는 느슨한 요약의 범위 안이다. 초안이 이 문장을 책의 관찰 안에 두고 있어 그대로 확정.

**42. 14장 스케일 3장면의 나머지** — 하시모토 혼자 일함·클로드 세션 3~4개·직접 읽고 되돌림·속도를 늦추더라도 '맛'(`chapter_14:362`), 스타인버거 세션 5~10개(`:364`), 인용 "I ship code I don't read(저는 코드를 읽지 않고 배포합니다)."(`:366`), 저자 해석 "실제 의미는 정반대에 가깝다 / 아키텍처의 최종 책임자는 어디까지나 본인"(`:368`), OpenAI 5개월 약 100만 줄·사람이 직접 쓴 코드 사실상 0줄(`:370`), 세 장면의 공통점 두 가지와 "달라지는 것은 단 하나, 하네스의 형태"(`:372`), 하시모토 하네스 = CLAUDE.md 한 장 + 워크트리 몇 개(`:374`).
- 확인 방법: `sed -n '340,380p' chapter_14_디버깅_RCA_팀.md` 전문 출력 후 문장 단위 대조. 인용문은 축자 일치. 외부 측 100만 줄·수동 코드 0줄은 1차 라운드 근거 F와 이번 2차 출처가 일치("humans never directly contributed any code").
- 본문 대비 정합성: 정확. 초안 "사람이 직접 쓴 코드는 사실상 0줄이었다"는 책의 "사실상 0줄에 가까웠습니다"보다 아주 조금 단단하지만, 외부 원문이 더 강하게 진술하므로 과장 아님. 스타인버거의 "월 6,600커밋"(`:364` 같은 문장)은 미해결 질문 7번에 따라 초안이 계속 회피 — 회피 확인(47번).

**43. 12장 신규 서술 일괄** — 로그인 기능 하나에 경계면 5개(`chapter_12:36`), SatangSlide는 Next.js 기반 서비스이고 표는 "실제로 드러난 런타임 버그"(`:38-48`), 본문에 옮긴 버그 3건(projects?.filter is not a function / thumbnailUrl vs thumbnail_url / 상태 전이 코드 누락으로 생성 페이지 영원히 대기), 인용 "이 버그들 중 어느 하나도 TypeScript 컴파일러가 잡지 못했습니다."(`:50`), 타입 시스템이 경계면 정합성을 보장 못 하며 전통적으로 사람의 일이었다(`:50`), "QA Phase는 만들지 않는다" + boundary-verifier Phase 2 상주·엔드포인트 완성 직후 검증(`:198`), 경계면 검증 7패턴 7행(`:223-231`).
- 확인 방법: `sed -n '30,55p'`·`sed -n '194,235p' chapter_12_풀스택_구현_팀.md`로 두 표 전문 출력 후 행 단위 대조. 7패턴은 초안의 나열 순서·문구가 원표 '패턴' 열과 순서까지 일치.
- 본문 대비 정합성: 정확. 인용 2건 축자 일치. 경계면 5개는 원문의 괄호 주석(폼(화면)·검증(훅)·API(서버)·JWT 서명(인증 미들웨어))을 뺀 나열이나 항목과 순서가 같아 왜곡 없음. "타입 시스템은 모듈 안쪽의 정합성은 보장해도 모듈 사이의 약속은 보장하지 못한다"에서 앞 절은 책에 없는 필자의 대비 서술이나 타입 시스템의 일반 성질이라 사실 문제 없음.

**44. 14장 판정 기준 신규 서술** — Fishbone 5카테고리(Code/Data/Config/Infra/Integration)(`chapter_14:191` 프론트매터, `:220` 본문), 목적은 '구조화된 브레인스토밍' 강제이며 카테고리마다 하나씩 요구하면 편향이 준다(`:220`), reproduction.sh 3회 연속 `exit 1`이어야 재현 성공 선언·플래키면 fixture 초기화 후 다시 3회(`:120-122`), 체크리스트의 "테스트를 수정·삭제하지 않았는가"는 절대 타협 불가이며 재현 테스트 삭제는 하네스 설계 실패의 결정적 증거(`:250-256`).
- 확인 방법: 네 구간을 각각 `sed`로 출력해 대조.
- 본문 대비 정합성: 정확. 초안의 "목적은 분류가 아니라 구조화된 브레인스토밍의 강제다"는 원문 "이 분류가 쓸모 있는 이유는 '구조화된 브레인스토밍'을 강제하기 때문입니다"의 수사적 압축이며, 원문이 분류·순위 매기기를 함께 언급하는 것과 충돌하지 않는다. 체크리스트 항목의 원문 괄호 "(Anthropic 규칙)"는 초안이 생략했으나 인용부호 안 문구는 그대로.

**45. 부록 B 신규 서술 일괄** — 도구 설계 4레버 표(인터페이스 = 재현을 요구하지 않고 참조 가능한 좌표 `appendix_b:124` / 개수 = 세션 시작 시 15개 상한 `:145` / 큐레이션 = 태스크별 동적 노출 `:159` / 피드백 = 무엇이·차이·어떻게 `:199-201`), 토큰 마비 3단계(컨텍스트 잠식·선택 혼동·연쇄 오류 `:138-142`), MCP 서버를 붙이면 미사용 도구 스키마까지 대기 상태로 컨텍스트를 잠식(`:150`), grep/search_code/find_references 예시(`:141`), "무비용 교육" — 훈련 데이터를 새로 구하지 않고도 한 번의 실패로 교정 루틴을 학습(`:203`).
- 확인 방법: `sed -n '112,210p' appendix_b_안티패턴_도구설계.md` 전문 출력 후 항목별 대조.
- 본문 대비 정합성: 정확. 초안이 "컨텍스트 잠식"에서 원문의 "400개 도구 스키마"를 "도구 스키마"로 일반화한 것은 수치 회피 방향이라 안전. **같은 `:138` 문장에 있는 "description+when_to_use 합산 1,536자 하드 상한"은 미해결 질문 8번(클로드 코드 API 표면의 현재성)에 따라 초안이 계속 회피** — 회피 확인(47번). "넷 중 셋은 덜어내는 방향"은 필자 해석이며 4레버의 성격과 모순 없음.

**46. 자기 참조 절(신설) 일괄** — 인용 "이 파일은 실제로 돌아가고 있는 오케스트레이터이고, 지금 이 장을 포함한 모든 장이 이 스킬의 지휘 아래 집필됐습니다."(`chapter_05:246`), bash 코드블록 2줄(`chapter_04:543-544`), 전원 opus·부모 세션 도구 통째 상속·운영 단순성 우선·파일 수준 가드레일 대신 시스템 프롬프트 수준 선언(`chapter_04:547`), 권장 설계 표 병기(`chapter_04:549-559`), 7장 자기 참조성(`chapter_07:109-111`).
- 확인 방법: `sed -n '240,250p' chapter_05_스킬_설계.md`, `sed -n '535,575p' chapter_04_에이전트_정의.md`, `sed -n '105,113p' chapter_07_메타_하네스_스킬.md` 실물 출력 후 대조.
- 본문 대비 정합성: 정확. bash 2줄은 주석(`# 8개 전부 model: opus`, `# 어떤 파일에도 tools 필드 없음`)까지 축자 일치 — 저장소 CLAUDE.md의 "코드 인용은 원문 그대로" 규칙 준수. **책 자체가 `chapter_04:537`에서 "7개의 에이전트"라 쓰고 코드 주석은 "8개"라 적어 내부 불일치가 있으나, 초안은 본문 산문에서 에이전트 수를 언급하지 않고 코드블록만 그대로 실어 이 불일치를 승계하지 않았다** — 초안 전체에 "7개의 에이전트"·"8개 에이전트" 표현 없음을 확인. 7장 "메타스킬이 자기가 정한 500라인 상한을 지킨다"는 500라인을 정한 주체가 형식상 2부(`chapter_07:109` "2부에서 정한")이나, 책이 같은 자리에서 "메타스킬은 자신이 정의한 규칙을 자기가 따릅니다"(`:111`)라 쓰고 2부 전체가 메타스킬 references에 접혀 있다는 구조를 근거로 삼으므로 책의 틀 안에서 정합.

**47. 의도적 회피 항목이 2차 보강본에도 섞여 들어가지 않음 (재확인)**
- 서지/출처: 초안 하단 작성자 노트 `[의도적 회피]` 줄 + 이 노트 미해결 질문 5·7·8·9·10·11번.
- 확인 방법: 발행 본문 구간만 잘라(`sed -n '1,302p'` → 작성자 노트 블록 제외) 다음 문자열을 전수 카운트: `37% / 1,206 / 1,196 / 1206 / 1196 / 잔 볼루크 / Bölük / Boluk / 6,600 / 6600 / HTekDev / 1,536 / 1536 / 2.2배 / 1.8배 / 1/19 / 6.7배 / $ / 8분 / 3분 / 2/4 / 4/4 / +60 / 917 / 52.8 / 66.5 / 68.3 / 36.2 / 23.8 / 42,486`.
- 본문 대비 정합성: **전부 0건.** 유일한 히트는 `49.5`·`79.3` 각 1건인데, 이는 1차 라운드에서 이미 확정한 1장 실험 층위 구분 문단(저자 내부 A/B임을 명시한 자리)의 기존 서술이다. 부록 D 가격·배수 계열은 `$` 포함 0건.
- **주의 — 1차 기록과의 차이:** 1차 검증 기록 16번은 "292개 에이전트 사건"을 회피 확인 항목으로 적었으나, **2차 보강본은 이 사건을 본문에 의도적으로 채택했다**(6장 절 + 상한선 절). 1차 기록 16번의 해당 항목은 2차부터 무효이며, 채택된 서술의 근거는 40번·35번이다.

**48. 문맥 변화로 기존 서술의 귀속이 어긋난 곳은 없음 (교차 점검)**
- 확인 방법: 보강으로 앞뒤 문맥이 바뀐 세 지점을 재확인했다. ⓐ 상한선 절의 "도구 개수는 최소화" → "도구 목록은 세션당 15개 안쪽"으로 바뀐 부분 — `appendix_b:145` "세션 시작 시 도구 목록이 15개를 넘지 않도록 상한을 둡니다"로 확정, 뒤이은 Vercel 15→2 사례(`chapter_04:249`, 1차 31번)와 층위 충돌 없음. ⓑ 상한선 절에 "292개 에이전트가 메모리를 삼킨 뒤에 그어진 선" 문장이 추가되며 앞선 6장 절과 같은 사건을 두 번 쓰는데, 두 곳 모두 "책이 인용한 사고 기록"이라는 귀속을 유지해 중복이 주장을 승격시키지 않음. ⓒ 결론부가 "이 명제를 뒷받침하는 실험들" → "근거들"로 바뀌고 "14장의 세 장면도 같은 계열 모델을 상수로 놓고 하네스의 형태만 비교한 관찰"이 추가된 부분 — 세 장면은 통제된 실험이 아니라 관찰이므로 총칭을 '실험'에서 '근거'로 넓히고 세 장면을 '관찰'로 부른 것이 오히려 정확해졌다(`chapter_14:372` "이 세 장면이 공유하는 공통점"). 1차 28번(Hashline "the weakest models gain the most")이 떠받치는 결론 논증도 그대로 성립.

### G. 2차 라운드 확인 불가 (0건)

없음. 초안의 `(확인 필요)` 1건은 외부 원문 확인으로 확정됐고(41번), 신규 서술의 외부 인용은 각주 추가 없이 기존 `[^openai]` 재사용뿐이라 1차 25번·근거 F의 검증 범위 안에 있다.

**다만 "검증됨"이 아니라 "책의 내부 주장으로 확정"인 항목이 하나 있음을 발행 전 유의할 것:**
- **292개 에이전트 / 36.8GB RSS / 메시지 큐 50개** — 공개 문서 없음(미해결 질문 3번). 초안은 이를 사실 주장이 아니라 "책이 6장에서 인용해 온 사고 기록"으로 명시해 처리했다. 이 귀속 표현(특히 "6장은 … 표기한다")을 editor가 문장 다듬기 과정에서 "책은"으로 되돌리면 8장 표기와 충돌해 다시 오류가 된다 — **손대지 말 것.**

---

## 검증 기록 — 3차 라운드 (구조 재작성본)

- 검증일: 2026-07-29 (같은 날 3차)
- 검증 대상: `_drafts/harness-engineering-book-overview.md` — 사용자 피드백("책 내용을 순서대로 설명하는 것처럼 읽힌다 / 장·부 번호 나열 대신 자연스러운 에세이로")에 따라 blog-writer가 **구조만** 재작성한 판. 기준선은 발행본 `_posts/2026-07-29-harness-engineering-book-overview.md`.
- 검증 전략: 이번 작업의 전제가 "사실은 그대로, 배치만 바꾼다"였으므로 **새 사실이 섞여 들어왔는지**를 1순위로 봤다. 문장 단위 diff·숫자 multiset 대조·인용문 전수 재대조라는 기계적 대조 3종을 먼저 돌린 뒤, 작성자 노트가 스스로 지목한 7개 지점을 원서 전사본으로 개별 재확인했다.
- 1차 근거: 전사 저장소 `/Users/sr/IdeaProjects/learning/books/harness-engineering-with-claude-code/output/chapters/`
- 2차 근거: 각주 URL 6건 HTTP 상태 재확인
- 초안의 `(확인 필요)` 플래그: **0건** (전수 grep)
- 결과 요약: **오류 교정 2건 / 확정 8건(항목 묶음) / 확인 불가 0건**
- **두 교정 모두 재작성이 만든 오류가 아니라 발행본에서 승계된 인용 오류다.** 즉 3차 재작성 자체는 사실을 훼손하지 않았고, 이번 라운드는 1·2차가 놓친 축자 이탈 2건을 뒤늦게 잡았다.

### H. 3차 라운드에서 오류로 판정해 교정한 것 (2건)

**49. 에어비앤비 사례에 대한 저자 재해석 인용 — 인용부호 안에서 빠진 "에이전트 팀의"와 쉼표 복원**
- 서지/출처: 황민호, 『하네스 엔지니어링 with 클로드 코드』, 한빛미디어, 2026, 13장 — `chapter_13_레거시_마이그레이션_팀.md:35` "얼핏 프롬프트 엔지니어링처럼 보이지만, 자세히 보면 에이전트 팀의 역할 분업 + 감독자 패턴 + 생성-검증 루프를 실제로 구현한 사례입니다."
- 확인 방법: 전사 저장소 전역 `grep -rn "얼핏 프롬프트 엔지니어링처럼"` → 히트 1건(`chapter_13:35`), 해당 행을 인용부호 안 문자열과 문자 단위 대조.
- 본문 대비 정합성: **원고가 근거와 다름(축자 이탈).** 발행본·3차본 모두 인용부호 안에서 주어 수식어 **"에이전트 팀의"**와 쉼표를 떨어뜨렸다. 하필 이 문단의 논지가 "앞에서 쌓아 둔 패턴 어휘로 다시 읽어내는 게 목적"이라, 역할 분업의 주체가 에이전트 팀이라는 것이 논지에 직접 걸린다. 원문대로 복원했고 복원이 오히려 문단의 주장을 강화한다. 초안에 근거 주석을 남겼다.
- 주의: 1·2차 라운드는 이 문장을 대조 대상으로 잡지 않았다(근거 C에 `chapter_13:35`가 요약돼 있었으나 인용부호 안 문자열 대조는 수행되지 않음). **1차 검증 기록에 "본문의 외부 인용 전량 대조"라 적힌 것은 각주가 붙은 외부 URL 인용에 한정된 진술이었다.**

**50. Vercel 결론 인용 — 생략된 괄호 주석 "(정적 마크다운)" 복원**
- 서지/출처: 앞의 책, 10장 — `chapter_10_등록과_진화.md:74` "Vercel 엔지니어링 팀의 결론은 명쾌합니다. \"단순무식한 접근법(정적 마크다운)이 오히려 더 정교한 스킬 기반 검색보다 우수한 성과를 냈다.\""
- 확인 방법: `sed -n '65,80p' chapter_10_등록과_진화.md`로 Vercel 표(5열 원표)와 결론 문장을 함께 출력해 대조.
- 본문 대비 정합성: **원고가 근거와 다름(괄호 생략).** 인용 자체의 의미는 보존되지만, 이 저장소의 포스트 규칙(`CLAUDE.md`)은 **"인용은 원문 그대로. 축약·괄호 생략 금지"**를 #30에서 명문화하고 있고, 그 규칙이 생긴 계기가 바로 "검증 가능한 사실은 그 자리에서 검증하라"가 결론인 글에서 괄호를 뺀 사건이었다. 이 글도 같은 부류의 글이라 같은 기준을 적용해 복원했다.
- 주의: 1차 검증 기록 14번은 이 문장을 "결론 문장 축자 대조 → 정확"으로 판정했으나 **괄호 누락을 놓친 오판**이다. 재사용 금지.

### I. 3차 라운드에서 확정한 것 (8건 묶음)

**51. 재작성이 새 사실을 들여오지 않았음 — 기계적 대조 3종**
- 확인 방법: ① **문장 단위 diff** — HTML 주석 제거 후 발행본↔3차본을 문장 단위 `difflib` 대조. 변경 블록 전량을 육안 확인한 결과 **모든 변경이 "N장/N부 → 주제 명칭" 치환 또는 소제목 교체**였고, 새로 추가된 사실 주장은 없다. ② **숫자 multiset 대조** — 두 본문의 숫자 토큰을 `Counter`로 빼기 연산. `draft − post = {}` (3차본에만 있는 숫자 0개). `post − draft`는 전부 장·부 번호(1~14)로, 스캐폴딩 제거의 결과다. ③ **블록인용 전수 대조** — `> ` 인용 15건이 발행본과 **한 글자도 다르지 않음**.
- 본문 대비 정합성: 확정. 구조 재작성이 사실 층위를 건드리지 않았다는 작성자 노트의 주장이 기계적으로 확인된다.

**52. 삭제된 직접 인용은 1건뿐이며 그 삭제가 타당함**
- 확인 방법: 인용부호 문자열을 양쪽에서 추출해 차집합. 발행본에만 있는 인용 문자열은 ⓐ "3장은 2부의 지도입니다"(`chapter_03:36`) ⓑ "이론 → 구성요소 → 자동화 → 사례 → 부록"(발행본 필자 표현, 인용 아님) ⓒ "검증자에게서 쓰기 권한을 빼앗는다"(발행본 소제목, 3차본에서는 따옴표 없는 소제목) 셋뿐.
- 본문 대비 정합성: 확정. 실제 삭제된 **책 인용은 ⓐ 1건**이고, 순수 목차 안내 문장이라 장·부 번호 제거 방침과 양립할 수 없다. 해당 문단의 논지(경계선만 긋고 각 기둥은 뒤에서 판다)는 `chapter_03:36` 본문이 그대로 뒷받침하므로 인용 없이도 성립. 3차본에만 있는 인용부호 문자열은 전부 `<!-- 검증: -->` 주석 안(원문 축자 기록)이라 발행 본문에는 늘지 않았다.

**53. 작성자 노트가 스스로 지목한 7개 지점 개별 재확인 — 전부 유지됨**
| 지점 | 근거 | 판정 |
|---|---|---|
| description 3단계 표 '목적' 열 3행 | `chapter_05:74-78` | **셀 단위 축자 일치.** 2차 라운드 33번이 복원한 "클로드가"/"클로드에게"가 그대로 살아 있다 |
| "…아무 스킬도 호출하지 않을 수 있다" 유보 | `chapter_05:90` | **유보 유지.** 단정으로 되돌아가지 않았다 |
| 292개 에이전트 인용의 출처 귀속 | `chapter_06:430` attribution 축자 / 충돌 지점 `chapter_08:271` "제가 직접 겪은 다음 예시" | **좁힘 유지.** 주어가 "6장은" → "오케스트레이터를 다루는 장은"으로 바뀌었을 뿐 지시 대상은 6장. "책은"으로 일반화되지 않았다. 같은 절 앞 문장의 "책은 이걸 이론으로 두지 않고 사고 기록 한 줄을 붙인다"는 **귀속 문장이 아니라 게재 사실 진술**이고 6·8장 모두에 게재되므로 "책은"이어도 충돌 없음 |
| OpenAI 하네스 구성 요약 | `chapter_14:374` | **"여러 층으로 쌓인 구조" 유지.** 2차 36번이 되돌린 표현이 "검증 구조"로 재축약되지 않았다 |
| 3인→7인 / 온보딩 비용 흡수 | `chapter_14:370` + OpenAI 회고 원문(2차 41번) | **귀속 구조 유지.** 인과 설명("온보딩 비용을 흡수")은 여전히 책의 진술로만 귀속되고, 그 뒤 "조직의 온보딩 비용을 옮겨 놓는 구조물"부터가 필자 해석으로 분리돼 있다 |
| 경계면 검증 7패턴 "거의 그대로" | 버그 표 `chapter_12:40-48`(7행) ↔ 패턴 표 `chapter_12:223-231`(7행) | **1:1 매핑 재수행 결과 2차 37번과 동일.** 버그2·버그7이 함께 패턴3에 대응하고 패턴7(옵셔널 필드 처리)에는 대응 버그가 없다 → 7 중 6. "거의 그대로"가 맞다 |
| book-writer 변경 이력 "서술" | `chapter_10:319`(산문 5항목) / 10장이 실제 인쇄한 표는 `:299-305`의 2026-04-05·07·10 일반 예시 | **"표"로 되돌아가지 않았다.** 3차본은 "함께 밝힌다 / 순으로 쌓였다고 적는다"로 서술 형태를 유지 |

**54. 수치 전수 재대조 — 원서 근거로 개별 확인**
- 확인 방법: 발행본 대조(51번)만으로는 "둘 다 틀렸을" 가능성을 배제하지 못하므로, 본문의 수치를 전사본 원문 행으로 다시 확인했다.
- 49.5 → 79.3 (+저자 내부 A/B): `chapter_01:69` / 292개·36.8GB·메시지 큐 50개: `chapter_06:430`, 상한의 유래 `chapter_08:275` / SKILL.md 443라인·references 6개 1,706라인·본문 약 4배·500라인 상한: `chapter_07:109` / Vercel 53%·53%(+0pp)·79%(+26pp)·100%(+47pp): `chapter_10:67-74`(원표는 Build·Lint·Test 열도 있는 5열, 초안은 '최종 Pass' 열만 발췌) / 도구 15개→2개·80%→100%: `chapter_04:249` / 팀 3~5명·7명 초과 시 Phase 분리·10명 이상 재구조화: `chapter_08:277-279`, 계층 2단계 `chapter_08:300`, 재시도 2~3회 `chapter_08:168`, SKILL.md 500줄 `chapter_05:386`, CLAUDE.md 150줄 `appendix_b:236`, 도구 15개 `appendix_b:145` / 로그인 경계면 5개 `chapter_12:36`, 런타임 버그 7종 `chapter_12:40-48` / 에어비앤비 3,500개·6주·97% `chapter_13:27` / 스킬 미호출 56% `chapter_05:27`, 스킬 목록 약 1% `chapter_05:33`, 스킬 30개×500자=15,000자 `chapter_05:132` / 결정론 게이트 3회 연속 `exit 1`·플래키 시 fixture 초기화 후 다시 3회 `chapter_14:120-122` / 하시모토 3~4세션·스타인버거 5~10세션·OpenAI 5개월 약 100만 줄·수동 0줄 `chapter_14:362-370`.
- 본문 대비 정합성: **전부 정확.** 원문보다 강해진 곳 없음. 7명 초과 항목은 원문이 "Phase 분리 **검토** — 공식 기준은 아닙니다"로 한정하는데 초안은 "페이즈를 나눈다"로 단정하나, 체크리스트 성격을 말하는 맥락이라 1차 21번의 판단(교정하지 않음)을 유지한다.

**55. 각주 7개 — 개수·URL·설명 모두 발행본과 동일하며 링크 생존**
- 확인 방법: 각주 정의 행을 양쪽에서 추출해 대조. HTTP 상태 재확인(`curl -sIL`): langchain 200 / vstorm(Hashline) 200 / vercel(agents-md) 200 / vercel(tools) 200 / openai.com **403** / medium.com **403**.
- 본문 대비 정합성: 확정. 각주는 `[^book]`·`[^openai]`·`[^langchain]`·`[^hashline]`·`[^airbnb]`·`[^vercel]`·`[^vercel-tools]` **7개**(8개가 아님)로 발행본과 동일. 403 두 건은 1차 라운드에서 이미 확인된 봇 차단이며(원문 존재는 검색 인덱스·2차 출처로 교차 확인, 1차 25번·2차 41번), URL 자체는 유효하다.

**56. `[^book]` 각주에서 한 문장을 뺀 것이 타당함**
- 서지/출처: 발행본 각주 원문 "…본문의 인용문은 모두 이 책에서 가져왔고, **위치는 괄호나 문장 안의 장 번호로 표시했다.**"
- 확인 방법: 3차본 발행 본문(1~292행, HTML 주석 제외)에 대해 `\d+장|\d+부` 정규식 전수 검색 → 히트 4건이 전부 **책 원문 인용 안**(`chapter_01:161`의 "2부", `chapter_07:45`의 "2부", `chapter_07:109`의 "2부", `chapter_08:421`의 "8장"·"4부")이거나 제목("5부")이다. 필자 서술의 장·부 번호는 **0건**.
- 본문 대비 정합성: 확정. 본문에 장 번호 표시가 사라졌으므로 그 문장을 남겨두면 **각주가 거짓 서술이 된다.** 삭제가 옳다.

**57. 장 번호를 주제 명칭으로 바꾼 지칭이 전부 올바른 장을 가리킴**
- 확인 방법: 치환된 지칭 표현을 하나씩 원서 목차·본문에 대응시켰다. "세 기둥을 갈라놓는 장"=3장(`chapter_03:34`) / "오케스트레이터를 다루는 장"=6장(팀 통신 프로토콜 회수 `chapter_06:226`, 사고 기록 `:430`) / "스킬 설계 장"=5장 / "패턴을 다루는 장"=8장(예고표 `chapter_08:423-429`) / "등록을 다루는 대목"·"등록과 진화를 다루는 자리"=10장 / "에이전트 정의를 다루는 대목"=4장(`chapter_04:543-544`) / "도구 설계를 다루는 부록"=부록 B / "토큰 경제를 다루는 부록"=부록 D / "코드 리뷰 팀 사례"=11장 / "풀스택 팀 사례"=12장 / "마이그레이션 사례"=13장 / "디버깅 팀"=14장.
- 본문 대비 정합성: 확정. 오지시 없음. 함께 확인한 3건: ⓐ "boundary-verifier를 **구현 단계**에 상주시킨다"의 Phase 2가 실제로 구현 단계임을 `chapter_12:83`("Phase 2 구현 팀")·`:91`("Phase 2(구현 + QA)")로 확인 — 축약이 정확하다. ⓑ "처방보다 먼저 오는 건 실습이다"는 1장이 5축 **목록**만 내놓고(`chapter_01:161`) 2장 실습을 거쳐 2부에서 구현을 다루는 실제 배치와 일치. ⓒ "책의 첫머리는 채드 파울러를 인용해"의 위치는 `chapter_01:128`로 1장이 맞다.

**58. 발행본에서 승계됐고 1차에서 이미 적발·허용된 "인용부호 안 압축" 3건 — 판단 유지**
- 대상과 근거: ⓐ "책임을 나누고 실행 시점에 맞물린다" ← `chapter_03:36` "하네스는 누가(Agent), 어떻게(Skill), 언제·누구와(Orchestrator)라는 세 가지 요소로 나뉘며, 이들은 독립적으로 설계되고 실행 시점에만 맞물린다." ⓑ "리뷰어가 코드를 직접 고치면 거짓 양성이 그대로 커밋되기 때문" ← `chapter_11:74` "이 에이전트들이 코드를 직접 고치면 검증 과정에서 발생한 거짓 양성(False Positive)이 그대로 커밋됩니다." ⓒ "tools 한 줄이 만드는 파일 수준 가드레일" ← `chapter_04:236-238`의 절 "가드레일 — 에이전트가 할 수 없는 것을 파일로 정하기"와 그 본문(프론트매터 tools 필드가 파일 자체에서 막는 층이라는 서술)을 요약한 필자의 명명.
- 확인 방법: 세 원문 행을 각각 실물 출력해 대조.
- 본문 대비 정합성: 셋 다 **축자 인용이 아니라 의미 정합한 압축·명명**이며, 1차 라운드 18·23번이 이미 같은 판정을 내리고 허용한 항목이다. 3차 재작성이 새로 만든 것이 아니므로 판단을 유지한다. 단 49·50번과 달리 **원문이 한두 낱말 차이로 가까운 경우가 아니라 명백히 재서술한 경우**라 축자 복원이 불가능하다는 점이 다르다. → 이후 라운드에서 재검토하려면 인용부호를 걷어내는 편이 옳고, 그것은 사실 판정이 아니라 표현 결정이므로 editor·사용자 몫이다.

### J. 3차 라운드 확인 불가 (0건)

없음. 본문의 외부 인용(각주 7건)과 책 인용 전량이 근거로 귀결됐다.

- **미해소로 남는 것은 1·2차와 동일하다:** 292개 에이전트 사건의 외부 검증(공개 문서 없음)과 그에 얽힌 **책 내부의 출처 표기 불일치**(`chapter_06:430` 인용 vs `chapter_08:271` "제가 직접 겪은"). 초안은 이를 사실 주장으로 승격시키지 않고 6장의 표기를 그대로 옮기는 방식으로 처리하고 있어 발행에 문제가 없다. 미해결 질문 3번 참조.

---

## 검증 기록 — 4차 라운드 (절 순서·서두 재작성본)

- 검증일: 2026-07-29 (같은 날 4차)
- 검증 대상: `_drafts/harness-engineering-book-overview.md` — Fable 모델의 두 차례 독립 피드백("장 지칭으로 절을 여는 패턴이 목차 순회처럼 읽힌다", "제목의 5부가 본문에서 증명 안 됨", "헛침 있음", "'수레다' 절이 목차 요약", "역산 문장 중복")을 반영해 blog-writer가 절 순서·서두 방식을 재작성한 판. 기준선은 발행본 `_posts/2026-07-29-harness-engineering-book-overview.md`.
- 검증 전략: 3차와 동일한 기계적 대조 3종(블록인용·숫자 multiset·문장 단위 차집합)을 먼저 돌려 **재배치가 사실을 훼손했는지**를 배제한 뒤, 이번 판이 **새로 만든 서술**(신규 인용 2건, 도입부 목차 서술, 헛침 해소 문단, 인용 이동 1건, 인용부호 제거 3건)을 원서 전사본으로 개별 확인했다.
- 1차 근거: 전사 저장소 `/Users/sr/IdeaProjects/learning/books/harness-engineering-with-claude-code/output/chapters/`
- 외부 근거: 이번 판은 각주·외부 인용을 1건도 추가·변경하지 않았으므로(65번) 1차 25~31번의 외부 검증 범위 안에 있다.
- 초안의 `(확인 필요)` 플래그: **0건** (전수 grep)
- 결과 요약: **오류 교정 1건 / 확정 9건(항목 묶음) / 확인 불가 0건**

### K. 4차 라운드에서 오류로 판정해 교정한 것 (1건)

**59. ★ 신규 문단("헛침 해소")에서 `model`을 파일 수준 가드레일로 묶은 것 — tools 전용으로 분리**
- 서지/출처: 황민호, 『하네스 엔지니어링 with 클로드 코드』, 한빛미디어, 2026, 4장.
  - `chapter_04_에이전트_정의.md:252` — "tools 필드는 '이 에이전트 하나'의 경계를 정하는 파일 수준 가드레일입니다. 그 위에는 OS·DB·네트워크 수준의 격리(컨테이너·VM·네트워크 차단)라는 또 다른 층이 있습니다."
  - `chapter_04:616`(정리) — "model·tools는 거버넌스 결정이지 성능 최적화가 아닙니다. **두 축은 독립되어 있으며 model은 비용과 추론 등급을, tools는 권한 경계를 결정합니다.** Write를 주지 않으면 본문을 수정할 수 없는 파일 수준 가드레일이 됩니다."
  - `chapter_04:236`(절 제목 "가드레일 — 에이전트가 할 수 없는 것을 파일로 정하기") — "에이전트가 아예 실행할 수 없도록 파일 자체에서 막아두는 층입니다. **프론트매터의 tools 필드가 그 역할을 합니다.** 자연어 지시는 안전장치가 아닙니다. 본문의 '## 작업 원칙'에 'Write 금지', '민감 파일 접근 금지'라고 써두는 건 프롬프트일 뿐입니다. … 확률적으로 판단하는 시스템에서는 판단이 흔들리는 순간이 반드시 옵니다."
- 확인 방법: 전역 `grep -rn "파일 수준 가드레일"` → 히트 4건 전부 `chapter_04`(`:252`, `:547`, `:567`, `:616`)이며 **모두 tools에 대한 서술**이고 model을 가드레일로 부르는 문장은 저장소 전체에 없다. 그 위에 `sed -n '225,265p'`로 가드레일 절 전문, `sed -n '600,625p'`로 정리 절 전문을 출력해 두 축의 분담을 확인.
- 본문 대비 정합성: **원고가 근거와 어긋남.** 초안은 "어떤 도구를 쥐여줄지(`tools`), 어떤 모델로 돌릴지(`model`)를 파일에 적는 순간, 그 에이전트가 할 수 **없는** 일이 … 파일 수준에서 정해진다. 책은 이걸 가드레일이라 부르고"라고 두 필드를 한 묶음으로 서술했다. `model`을 적는 것은 그 에이전트가 할 수 **없는** 일을 정하지 않는다 — 책은 `:616`에서 두 축을 명시적으로 갈라 놓는다(model = 비용·추론 등급 / tools = 권한 경계). 하필 이 문단이 이번 판의 목적("계약서 쪽 계산을 실제로 펼친다")을 떠받치는 자리라, 계산의 첫 단추가 틀리면 뒤의 인용(`:616`)이 근거 없이 서 있게 된다.
- 교정 내용: 가드레일 주장을 `tools`에만 걸고, `model`을 "권한이 아니라 비용과 추론 등급을 가르는 별개의 축"으로 분리한 뒤 "결론은 둘을 함께 묶는다"로 인용에 연결했다. 함께 넣은 "본문에 'Write 금지'라고 써 두는 건 프롬프트일 뿐이고, 확률적으로 판단하는 시스템에서 판단은 언젠가 흔들린다"는 `:236` 축자 근거로 초안이 이미 주장하던 "프롬프트가 아니라 파일 수준에서"를 떠받치기 위해 덧붙인 것이다. 초안에 근거 주석을 남겼다.

### L. 4차 라운드에서 확정한 것 (9건 묶음)

**60. ★ 신규 채택 인용 2건 — 축자 일치, 리서치 노트 근거와 좌표·문맥 모두 부합**
- 대상: 본문 "파일은 다섯 개인데 개념은 셋이다" 절의 블록인용 2건.
- 서지/출처:
  - `chapter_04_에이전트_정의.md:58` (절 「에이전트 정의 파일 = 역할 계약서」) — "에이전트 정의 파일은 단순한 설정 파일이 아니라 **역할 계약서**입니다. … '설정'보다 '계약서'가 정확한 이유는 간단합니다. **설정 파일은 값을 저장하지만 계약서는 약속을 저장합니다.** description 한 줄이 호출 시점을 바꾸고 tools 한 줄이 부작용 범위를 바꿉니다."
  - `chapter_04_에이전트_정의.md:616` (절 「[p.90] 정리」) — 위 59번에 전문 인용.
- 확인 방법: 전역 `grep -rn "설정 파일은 값을 저장하지만"` → 히트 1건(`chapter_04:58`), `grep -rn "거버넌스 결정이지"` → 히트 1건(`chapter_04:616`). 두 행을 초안 인용부호 안 문자열과 문자 단위 대조.
- 본문 대비 정합성: **두 건 모두 축자 일치**(마침표 포함). 리서치 노트 근거 C(4장)의 두 항목이 정확히 이 좌표를 적어 두었고(`:70`, `:74`행), 문맥도 맞는다 — `:58`은 "왜 설정 파일이 아니라 계약서인가"에 대한 답이고 `:616`은 그 장의 정리 문장이라 초안이 전개의 시작과 결론으로 각각 배치한 것이 원서 구조와 일치한다. **근거 사슬 게이트 통과.**
- 부기: 초안이 인용 사이에 놓은 "그 약속의 실체는 프론트매터 한두 줄이다"는 `:58`의 후속 문장("description 한 줄이 호출 시점을 바꾸고 tools 한 줄이 부작용 범위를 바꿉니다")과 부합한다. 다만 책이 그 자리에 드는 예시 쌍은 description+tools이고 초안은 tools+model이라, 그 갈래에서 59번의 교정이 필요했다.

**61. 도입부 "목차 5부" 신규 서술 — 근거 8번과 일치하며 오도 없음**
- 대상: "목차 쪽 사실부터 적어 두면 이렇다. 다섯 덩어리다. 왜 하네스인가라는 진단, 무엇으로 구성하는가라는 분해, 그 구성을 어떻게 자동화하고 유지하는가, 네 도메인의 실전 적용, 그리고 부록 네 편. 마지막 덩어리까지 번호를 받아 Part 05로 적혀 있다."
- 서지/출처: 목차 표기 `00_front_matter.md:182`(**Part 01 하네스란 무엇인가**), `:214`(**Part 02 하네스 구성하기 — 에이전트·스킬·오케스트레이터**), `:327`(**Part 03 하네스 스킬로 구성하기**), `:445`(**Part 04 하네스 실전편**), `:532`(**Part 05 부록**). 산문 쪽 `00_front_matter.md:149` — "본문은 총 4부, 14장으로 이루어져 있습니다. 추가 부록은 총 4개입니다." 각 Part의 목적 서술은 `:151`(1부 = 왜 하네스인가), `:153`(2부 = 세 기둥 구성), `:159`(3부 = 2부의 단계를 자동화하는 메타스킬 + 등록·drift 유지), `:161`(4부 = 4개 도메인 사례), `:163`(부록 4편).
- 확인 방법: `grep -n "^Part\|Part 0\|PART" 00_front_matter.md`로 Part 표기 5건을 전수 추출(1차 검증 기록 8번의 재수행). `sed -n '145,170p'`로 「이 책의 구성」 산문 전문을 출력해 다섯 덩어리의 요약 문구를 1:1 대조.
- 본문 대비 정합성: **정확하며 오도 없음.** ① 다섯 요약구가 각각 Part 01~05의 산문 설명과 일치한다. ② "Part 05로 적혀 있다"는 목차 인쇄 사실이고 `:532`가 문자 그대로 그렇게 인쇄한다. ③ **산문(`:149`)이 "4부 + 부록 4개"로 세는 것과의 불일치를 밟지 않는다** — 초안은 이 문장을 "목차 쪽 사실부터 적어 두면"으로 열어 범위를 목차로 한정하고, "마지막 덩어리**까지** 번호를 받아"라는 조사로 부록이 원래 파트가 아님을 이미 함의한다. 게다가 같은 도입부 앞 문단이 "14개 장과 부록 4편을 덮고"라 적어 산문 쪽 계수도 함께 싣고 있다. 제목의 대비("목차는 5부인데")도 목차로 한정돼 있어 책이 스스로 "5부"라고 주장한다는 오해를 만들지 않는다.

**62. 292개 에이전트 사건 귀속 — 6장으로 정확히 좁혀져 있고 8장과 충돌 없음**
- 서지/출처: `chapter_06_오케스트레이터.md:430` — `> "세션 9a990de8**이** 2분 내 292개 에이전트를 생성하여 36.8GB RSS에 도달했습니다. 이 사례 이후 팀원 한 명이 받을 수 있는 메시지 수가 최대 50개로 제한되었습니다." **— 클로드 코드 내부 사고 사례, 오케스트레이션 패턴 가이드**`. 충돌 지점 `chapter_08_아키텍처_패턴.md:271-273` — "제가 직접 겪은 다음 예시를 보겠습니다." + `> "세션 9a990de8**가** …"`(**attribution 없음**).
- 확인 방법: `grep -n "9a990de8" chapter_06_*.md chapter_08_*.md` → 히트 2건(6장 `:430`, 8장 `:273`). 두 행을 초안 인용문과 문자 단위 대조. 8장 앞뒤(`sed -n '265,305p'`)를 출력해 attribution 유무를 확인.
- 본문 대비 정합성: **확정.** ① 초안 인용문은 조사까지 6장 판과 일치(`9a990de8이`)한다. ② 6장 판에만 attribution이 붙고 8장 판에는 없으므로, "인용 끝에는 출처가 …로 붙어 있다"는 **초안이 싣는 그 게재분에 대해 참**이며 8장의 "제가 직접 겪은"과 정면 충돌하지 않는다(8장은 경쟁하는 표기를 인쇄하는 게 아니라 표기를 생략한다). ③ 뒤따르는 추론이 "**이 표기대로라면** 저자가 돌린 실험이 아니라 인용해 온 사고 기록이다"로 한정돼 있어 사실 주장으로 승격되지 않는다. ④ 앞 문장 "책은 이걸 이론으로 두지 않고 사고 기록 한 줄을 붙인다"는 게재 사실 진술이고 6·8장 모두에 게재되므로 "책은"이어도 무방하다(3차 53번 판단 유지). **editor·publisher는 "책은 이 인용의 출처를 …로 표기한다" 같은 일반화로 되돌리지 말 것.**

**63. "3명의 집중된 팀원…" 인용의 블록 승격과 위치 이동 — 원문 문맥에 오히려 더 가까움**
- 서지/출처: `chapter_06_오케스트레이터.md:444` — `> "3명의 집중된 팀원이 5명의 산만한 팀원보다 낫습니다." — 메타스킬 공식 SKILL.md`. 원서에서 이 인용의 직전 문맥: `:425`("SendMessage가 full mesh를 허용하는 만큼 … 4명일 때 6개는 감당 가능하지만, 10명이 되면 45개가 됩니다. **각 팀원이 나머지 9명의 메시지를 수신하고 적절히 반응하려면 자기 컨텍스트 윈도의 상당 부분을 메시지에 빼앗깁니다. 즉, 실제 작업에 쓸 컨텍스트가 줄어듭니다.**") → `:430`(292 인용) → `:432`("클로드 코드가 팀원 메시지 큐를 50개로 제한한 이유가 여기 있습니다. 팀을 너무 크게 만들면 메모리가 한계에 도달하고, **메시지 큐가 무관한 컨텍스트로 가득 차며**, 오히려 품질이 떨어집니다.") → 권장 팀원 수 표(소규모 2~3 / 중규모 3~5 / 대규모 5~7) → `:444`(인용).
- 확인 방법: `sed -n '415,455p' chapter_06_오케스트레이터.md`로 절 전문을 출력해 인용의 전후 문맥을 확인. 발행본↔초안의 문자열 대조로 승격 전후 표기 차이를 확인.
- 본문 대비 정합성: **확정, 왜곡 없음.** ① 축자성이 오히려 개선됐다 — 발행본은 인라인이라 문장 끝 마침표가 빠져 있었고(`"…보다 낫습니다"`), 초안의 블록인용은 원문대로 마침표를 포함한다. ② 문맥 정합성도 개선됐다 — 원서에서 이 인용은 "메시지가 각자의 컨텍스트를 잠식한다 → 그래서 팀은 작아야 한다"는 흐름 끝에 놓이는데, 초안의 새 자리("컨텍스트는 공공재이고, 팀원을 한 명 늘리는 건 나머지 전원의 몫에서 조금씩 떼어내는 일이다. 그래서 이 문장이 성립한다")가 바로 그 흐름이다. 발행본의 자리(상한선 절, "이 성격을 요약한다")보다 원문 논지에 가깝다. ③ "컨텍스트는 공공재"는 5장 문장(`chapter_05:132`)의 회수인데 초안이 "앞서 스킬을 두고 한 말이 팀 규모에서 그대로 다시 나온다"로 이월을 명시하므로 출처 혼동이 없고, 6장 쪽 사실 근거(`:425`·`:432`)가 따로 있어 유비가 공중에 뜨지 않는다. ④ 원서는 인용 끝에 "— 메타스킬 공식 SKILL.md"를 붙이지만 이 표기는 주장의 귀속을 바꾸지 않으므로(292 인용과 다른 점) 다른 책 인용과 동일하게 각주 `[^book]`로 처리한 것이 일관된다. **상한선 절로 되돌리지 말 것** — 초안에 근거 주석을 남겼다.

**64. 인용부호 3건 제거 — 실제로 걷혔고, 남은 평서문이 원문 압축으로 오인될 소지 없음**
- 대상과 판정(3차 58번이 "표현 결정이라 editor·사용자 몫"으로 남긴 3건):
  - ⓐ 발행본 `방금 그은 경계선("책임을 나누고 실행 시점에 맞물린다")의 실연처럼 읽힌다` → 초안 `책임을 나누되 실행 시점에만 맞물린다는 방금의 경계선을 실연하는 것처럼 읽힌다`. **인용부호 없음.** 게다가 문구가 `chapter_03:36`("이들은 독립적으로 설계되고 **실행 시점에만** 맞물린다")의 "**에만**"을 되살려 원문에 더 가까워졌다. 같은 절 앞부분이 이미 이 명제를 필자 문장으로 풀어 두었으므로("셋은 독립적으로 설계되다가 실행 시점에만 맞물린다") 되받기로 읽히지 인용으로 읽히지 않는다.
  - ⓑ 발행본 `이유는 "리뷰어가 코드를 직접 고치면 거짓 양성이 그대로 커밋되기 때문"` → 초안 `리뷰어가 코드를 직접 고치면 검증 과정에서 나온 거짓 양성이 그대로 커밋되기 때문이다`. **인용부호 없음**이고 종결어미(`~때문이다`)를 붙여 문장으로 닫았다. `chapter_11:74`("이 에이전트들이 코드를 직접 고치면 **검증 과정에서 발생한** 거짓 양성(False Positive)이 그대로 커밋됩니다")의 "검증 과정에서"를 되살려 압축의 정보 손실도 줄었다. 인용부호를 벗었으므로 "발생한 → 나온" 차이는 문제되지 않는다.
  - ⓒ 발행본 `앞에서 "tools 한 줄이 만드는 파일 수준 가드레일"이라 부른 것` / `스스로 도달한 결론("tools 한 줄이 만드는 파일 수준 가드레일")` → 초안 `앞에서 본 tools 한 줄의 가드레일` / `tools 한 줄로 가드레일을 세우라는 자기 결론`. **두 곳 모두 인용부호 없음.** 후자가 책의 결론인지도 확인했다 — `chapter_04:567` "지금은 이 경계가 시스템 프롬프트에 선언되어 있지만, **tools 필드로 옮기면** 프롬프트를 잘못 해석하는 경로까지 방지할 수 있습니다."
- 확인 방법: 발행본↔초안 문장 단위 차집합으로 세 지점을 특정한 뒤 각 원문 행(`chapter_03:36`, `chapter_11:74`, `chapter_04:236-238`·`:567`)을 실물 출력해 대조. 초안 본문 전체의 인용부호 문자열을 재추출해 세 문구가 남아 있지 않음을 확인.
- 본문 대비 정합성: **확정.** 세 곳 모두 인용부호가 실제로 걷혔고, 남은 평서문은 필자 서술 문장으로 읽히지 원문 압축으로 오인될 형태가 아니다. 3차 58번의 권고가 이행됐다.
- 반면 **유지가 옳은 것 1건**: `"스스로 고쳐서 통과 처리하는 문제를 구조적으로 차단"`(마이그레이션 팀 항목)은 그대로 두었고 이것이 맞다. `chapter_13:283` 원문은 `검증자에게 Edit가 없는 이유는 "스스로 고쳐서 통과 처리"하는 문제를 구조적으로 차단하기 위함입니다.`로, 책의 내부 인용부호는 "…통과 처리"에서 닫히지만 **초안이 인용부호로 묶은 문자열 전체가 원문의 연속된 부분문자열**이라 인용으로서 정확하다.

**65. 재배치가 사실을 훼손하지 않았음 — 기계적 대조 4종**
- 확인 방법: HTML 주석 제거 후 발행본↔초안을 대조. ① **블록인용** — 발행본 15건이 **한 글자도 다르지 않게 전부 생존**. 초안에만 있는 3건은 신규 2건(60번)과 인라인→블록 승격 1건(63번). ② **숫자 multiset** — `draft − post = {'05': 1}`(Part 05, 61번), `post − draft = {'4': 1, '13': 1, '50': 1}`. 셋 다 유실이 아니라 문장 병합의 결과다: `13`·`4` = "뒤의 13개 장과 부록 4편" → "나머지 전부가"(도입부에 이미 있는 계수의 중복 제거), `50` = 발행본이 6장 절과 상한선 절에 나눠 쓴 역산 서술을 상한선 절 한 곳으로 합치면서 `"50개면 충분하다"` 한 번이 줄어든 것(사실은 상한선 절에 그대로 남아 있다). **새 숫자·유실된 사실 0건.** ③ **표·코드블록** — 표 17행(3개 표)과 bash 코드블록 1개가 발행본과 **완전 동일**. ④ **각주** — 정의 7개가 문자 단위로 동일하고 본문 참조 횟수도 동일(`[^openai]`·`[^langchain]` 3회, 나머지 5개 2회).
- 본문 대비 정합성: 확정. **이번 판이 외부 인용을 1건도 추가·변경하지 않았으므로 1차 25~31번·근거 F의 외부 검증이 그대로 유효하다.**

**66. 문장 단위 차집합 전량 검토 — 재배치가 만든 새 사실 진술은 59·60·61번이 전부**
- 확인 방법: 발행본·초안을 문장 단위로 쪼개 양방향 차집합(발행본 전용 111문장 / 초안 전용 110문장)을 뽑아 육안 전수 확인.
- 본문 대비 정합성: 확정. 차집합의 대부분이 ⓐ 장 지칭 서두를 질문·수치·장면으로 바꾼 치환("오케스트레이터 장의 출발점은 이렇다" → "팀원이 4명이면 잠재 통신 채널은 6개다"), ⓑ 소제목 교체, ⓒ 연결부 산문 압축이다. 이 중 **사실 층위를 건드린 것은 세 곳뿐**이고 각각 59·60·61번으로 처리했다. 함께 확인한 부수 변경 4건:
  - **리플릿 고유명 제거** — 발행본 "프로덕션 DB를 삭제하고 복구 불가라고 거짓 보고까지 한 리플릿 사건" → 초안 "에이전트가 프로덕션 DB를 지웠고, 곧바로 복구가 불가능하다고 보고했다. 그 보고도 사실이 아니었다." 사건명을 뺐을 뿐 서술은 `chapter_01:32-34`와 일치한다("9일째 되던 날 갑자기 프로덕션 데이터베이스가 사라졌습니다 … 에이전트는 **직후** '데이터 복구가 불가능합니다'라고 보고했습니다. 하지만 렘킨이 수동으로 확인해 보니 복구는 가능했습니다. 거짓 보고였던 것입니다"). "곧바로" = 원문 "직후". 고유명 제거는 미해결 질문 9번(레코드 건수 표기 불확실)을 더 멀리 회피하는 방향이라 안전.
  - **4부 배치 서술의 패턴명 보강** — 발행본 "가장 익숙한 문제(리뷰)에서 팬아웃을 … 복합 패턴을 … 결정론 게이트를" → 초안 "팬아웃·팬인을 … 계층적 위임과 파이프라인이 겹친 복합 패턴을 … 생성-검증과 결정론 게이트를". `chapter_08:423-429` 예고표와 대조: 11장=파이프라인+**팬아웃·팬인**, 12장=**계층적 위임+파이프라인**, 13장=감독자(동적 분배), 14장=**생성-검증**+팬아웃. **네 항목 모두 표와 일치**하며 초안이 표를 옮겨 적지 않고 문장으로 흡수했다(3-b의 "목차 요약 제거"). 14장에 덧붙인 '결정론 게이트'는 표의 패턴명은 아니지만 `chapter_14:120-122`가 그 장의 장치로 명시하므로 사실 오류 아님. 다만 "**패턴 커버리지 순이다**"라는 배치 원리 자체는 책이 명시한 문장이 아니라 **필자의 해석**이다(예고표는 장별 패턴만 밝히고 순서의 이유는 밝히지 않는다) — 발행본에서 승계된 서술이고 초안이 이를 저자 진술로 귀속하지 않으므로 유지.
  - **"전부 사고에서 역산됐다" 완화** — 발행본 "이 값들의 공통점은 **전부 사고에서 역산됐다**는 것이다" → 초안 "이 값들의 공통점은 만들어진 **방향**이다. 무엇을 넣으면 좋아지는지가 아니라, 어디를 넘으면 무너지는지에서 나왔다." 나열된 상한 중 SKILL.md 500라인·CLAUDE.md 150줄·도구 15개는 사고 기록이 붙어 있지 않으므로 **완화가 오히려 정확해졌다**(1차 21번이 근거로 든 `chapter_08:275` "이 사건 이후 다음 규칙을 세웠습니다"는 팀 크기 규칙에 한정된다).
  - **메타스킬 500라인 귀속** — 발행본 "앞에서 제시한 500라인 상한" → 초안 "책이 앞서 정한 500라인 상한". `chapter_07:109` "2부에서 정한 현실적인 최대치인 500라인"과 부합(2차 46번의 판단 유지).

**67. 3차까지 복원·좁혀 둔 7개 지점이 이번 재작성에서도 유지됨**
- 확인 방법: 3차 53번의 표를 그대로 재수행하고, 초안에 남은 인라인 `<!-- 검증: -->` 주석 10건이 각각 바뀐 문장을 여전히 올바르게 지시하는지 대조했다.
- 판정: **7개 전부 유지.** description 3단계 표의 "클로드가"/"클로드에게"(`chapter_05:74-78`) 셀 단위 일치 / "…호출하지 않을 수 있다" 유보(`chapter_05:90`) 유지 / 292 귀속 좁힘 유지(62번) / OpenAI 하네스 "여러 층으로 쌓인 구조"(`chapter_14:374`) 유지 / 3인→7인의 인과 설명을 책에 귀속하고 필자 해석을 분리한 구조(`chapter_14:370` + OpenAI 회고) 유지 / 경계면 7패턴 "거의 그대로"(`chapter_12:40-48` ↔ `:223-231`, 7 중 6) 유지 / book-writer 변경 이력은 "표"가 아니라 서술(`chapter_10:319`) 유지. 에어비앤비 인용의 "에이전트 팀의"(3차 49번)와 Vercel 인용의 괄호 "(정적 마크다운)"(3차 50번)도 축자 그대로 살아 있다.

**68. 수치·인용 표본 재대조 — 원서 근거로 개별 재확인**
- 확인 방법: 65번의 발행본 대조만으로는 "둘 다 틀렸을" 가능성을 배제하지 못하므로, 이번 판에서 문맥이 바뀐 절의 수치를 전사본 원문 행으로 다시 출력해 확인했다.
- 결과: 스킬 미호출 56% `chapter_05:27` / 스킬 목록 약 1% `chapter_05:33` / 스킬 30개 × description 500자 = 매 요청 15,000자 `chapter_05:132` / description 3단계 표 `chapter_05:74-78` / 경계 조건 유보 `chapter_05:90` / 4명 6개·10명 45개·N×(N-1)/2와 메시지가 컨텍스트를 잠식 `chapter_06:425` / 292·36.8GB·큐 50개 `chapter_06:430`, 상한의 유래 `chapter_06:432`·`chapter_08:275` / 팀 3~5명 `chapter_06:434-440`·`chapter_08:277-279` / SKILL.md 443라인·references 6개 1,706라인·본문 약 4배·500라인 상한 `chapter_07:109` / 8장 예고표와 "지도/지형" 인용 `chapter_08:421-429` / 세션 시작 시 도구 15개 상한 `appendix_b:145` / 저자 내부 A/B 49.5→79.3 `chapter_01:69` — **전부 정확하고 원문보다 강해진 곳 없음.** 다만 `chapter_05:27`이 56%에 붙인 단서("그 원인이 description이 모호해서인지 … 명확히 밝히지 않았습니다")는 초안이 여전히 옮기지 않는데, 초안의 문장("본문이 부족해서가 아니라 본문이 열리기도 전에 막힌 것이다")은 원인 규명이 아니라 **미호출이면 본문이 열리지 않는다**는 정의상 참인 진술이라 2차 39번의 판단을 유지한다.

**69. 의도적 회피 항목이 이번 판에도 섞여 들어가지 않음 (재확인)**
- 확인 방법: 65번의 숫자 multiset 대조가 곧 전수 확인이다 — 초안에만 있는 숫자 토큰이 `05` 하나뿐이므로, 발행본이 이미 회피를 통과한 이상(1차 16번·2차 47번) 새로 섞여 들어온 회피 대상 수치는 **구조적으로 0건**이다.
- 판정: 확정. 부록 D 절대 가격표·Haiku/Opus 배수, 준수율 37%, 리플릿 레코드 건수, "잔 볼루크" 표기, 코드 리뷰 팀 Without/With 개별 수치, 스타인버거 월 6,600커밋, HTekDev 조사 수치, description+when_to_use 1,536자 상한, 프론트매터 공식 16개 필드 수치 — 전부 본문에 없다.

### M. 4차 라운드 확인 불가 (0건)

없음. 본문의 외부 인용(각주 7건)은 이번 판에서 변경되지 않아 1차 25~31번의 검증이 그대로 유효하고, 신규·변경 서술은 전부 원서 전사본으로 귀결됐다.

- **미해소로 남는 것은 1~3차와 동일하다:** 292개 에이전트 사건의 외부 검증(공개 문서 없음)과 **책 내부의 출처 표기 불일치**(`chapter_06:430` attribution vs `chapter_08:271` "제가 직접 겪은"). 초안은 이를 사실 주장으로 승격시키지 않고 6장 게재분의 표기를 그대로 옮기는 방식으로 처리하고 있어 발행에 문제가 없다. 미해결 질문 3번 참조.

### N. publisher에게 (발행 시 주의 1건)

- 본문 "이 책의 유일한 공리는 …" 절에 **코드 스팬 안의 리터럴 HTML 주석**이 있다 — `` `<!-- -->`로 감싼 순간 그 규칙은 컨텍스트에서 사라지고 ``. `<!--.*?-->` 류의 단순 정규식으로 주석을 걷으면 이 문장이 깨진다. 지난 발행에서는 온전히 살아남았으니 같은 방식을 유지할 것.

---

## 검증 기록 — 5차 라운드 (독립 재검증)

- 검증일: 2026-07-30
- 검증 대상: `_drafts/harness-engineering-book-overview.md` (4차 절 순서·서두 재작성본 = 4차 라운드와 동일한 초안). 기준선은 발행본 `_posts/2026-07-29-harness-engineering-book-overview.md`.
- **이 라운드의 성격:** 4차 라운드 기록(59~69번)이 이미 존재하는 상태에서 **같은 초안을 독립적으로 다시 검증**했다. 4차 기록을 근거로 재사용하지 않고 원서 전사본에서 다시 확인했다. 이 방침을 택한 이유는 이 노트 자체가 앞서 두 번 "이전 라운드의 판정이 오판이었다"를 기록했기 때문이다(3차 50번이 1차 14번을 뒤집고, 검증 기록 7번이 근거 D·인사이트 7을 뒤집음). 실제로 이번에도 **4차 판정 1건이 뒤집혔다**(70번).
- 1차 근거: 전사 저장소 `/Users/sr/IdeaProjects/learning/books/harness-engineering-with-claude-code/output/chapters/`
- 외부 근거: 이번 판은 각주·외부 인용을 1건도 추가·변경하지 않았다(각주 정의 7개가 발행본과 문자 단위 동일, 76번). 따라서 1차 25~31번·근거 F의 외부 검증이 그대로 유효하며 이번 라운드에서 외부 재조회는 하지 않았다.
- 초안의 `(확인 필요)` 플래그: **0건** (전수 grep)
- 결과 요약: **오류 교정 3건 / 확정 7건(항목 묶음) / 확인 불가 0건**

### O. 5차 라운드에서 오류로 판정해 교정한 것 (3건)

**70. ★ 마이그레이션 팀 인용 — 원문의 내부 닫는 따옴표를 지워 없는 문자열을 인용으로 제시한 것 (4차 64번 판정 뒤집음)**
- 서지/출처: 황민호, 『하네스 엔지니어링 with 클로드 코드』, 한빛미디어, 2026, 13장 — `chapter_13_레거시_마이그레이션_팀.md:283` 원문: `검증자에게 Edit가 없는 이유는 "스스로 고쳐서 통과 처리"하는 문제를 구조적으로 차단하기 위함입니다.`
- 확인 방법: 초안이 인용부호로 묶은 문자열 전체를 `grep -c -F "스스로 고쳐서 통과 처리하는 문제를 구조적으로 차단" chapter_13_레거시_마이그레이션_팀.md` → **히트 0건.** 이어서 `sed -n '283p' | tr '.' '\n'`로 해당 문장을 분리 출력해 책의 인용부호가 "통과 처리"에서 닫히는 것을 실물 확인.
- 본문 대비 정합성: **원고가 근거와 다름.** 책은 `"스스로 고쳐서 통과 처리"`까지만 인용부호로 묶고 `하는 문제를 구조적으로 차단하기 위함입니다`는 저자의 산문이다. 발행본과 4차본은 그 **내부 닫는 따옴표를 삭제해 뒤 산문까지 인용 안으로 끌어왔고**, 그 결과 원문에 존재하지 않는 문자열이 축자 인용의 외형을 갖게 됐다. 이 저장소의 포스트 규칙(`CLAUDE.md` #30: "인용은 원문 그대로. 축약·괄호 생략 금지")이 3차 49·50번에서 더 작은 이탈(수식어 1개, 괄호 1개)까지 교정한 기준이므로 같은 기준을 적용해 책이 묶은 범위대로 복원했다.
- 교정 내용: `"스스로 고쳐서 통과 처리"하는 문제를 구조적으로 차단한다`. 같은 목록의 다른 세 항목이 종결어미로 끝나므로 형식도 어긋나지 않는다.
- **선행 기록 정정 2건 — 재사용 금지:** ① **4차 검증 기록 64번**은 이 문자열을 "**원문의 연속된 부분문자열**이라 인용으로서 정확하다"고 판정하고 "유지가 옳은 것 1건"으로 명시했다. **오판이다** — 내부 따옴표 2개를 지워야만 연속이 된다. ② 이 노트 **「인사이트 후보」 4번**(13장 항목)도 같은 병합형을 싣고 있다. 두 곳 모두 그대로 인용하지 말 것.

**71. ★ "책이 마지막에 하는 일" — 4차본이 새로 만든 위치 주장이 거짓**
- 서지/출처: Build to Delete와 ADR '삭제 조건' 필드는 `chapter_10_등록과_진화.md`에만 있다 — `:392`(절 제목 "심화 — ADR과 Build to Delete"), `:406`("5. 삭제 조건 — 언제 이 결정이 불필요해지는가"), `:408`, `:421`(예시), `:426`("**Build to Delete**는 사라져야 할 시점(조건)을 설계에 포함하는 원칙입니다"). 책의 실제 뒷부분: 목차상 `00_front_matter.md:532` **Part 05 부록** → `:618` 마치며 _304 → `:619` 찾아보기 _306. 즉 10장 뒤로 11~14장 + 부록 4편 + 「마치며」가 이어진다.
- 확인 방법: 전역 `grep -rn "Build to Delete\|삭제 조건"` → 히트가 `00_front_matter.md:431`(목차)과 `chapter_10`(5행)뿐이고 **14장·부록·「마치며」에는 0건.** 이어서 `closing_마치며.md` 전문(32행)을 출력해 폐기 조건 서술이 없음을 확인.
- 본문 대비 정합성: **원고가 근거와 어긋남.** 4차본은 절을 "하네스를 잘 만드는 법을 가르친 책이 **마지막에** 하는 일은 자기 폐기 조건을 적는 것이다"로 열었는데, 해당 내용은 3부 마지막 장(10장)이지 책의 끝이 아니다. 이 판은 장·부 지칭을 전부 없애는 방향으로 재작성됐으므로(4차 노트 [1]) 독자가 위치를 보정할 단서도 없다. 게다가 **같은 절 끝이 「마치며」를 따로 인용**하므로 절 안에서 "마지막"이 서로 다른 두 곳을 가리키게 된다. 발행본의 대응 문장("가장 성숙하다고 느낀 것도 같은 자리, 등록과 진화를 다루는 대목이다")은 위치를 정확히 10장으로 짚었으므로, **장 지칭을 제거하는 과정에서 새로 생긴 오류**다.
- 교정 내용: 위치 주장을 없애고 책의 처방으로 낮췄다 — "그 하네스를 언제 버려야 하는지도 함께 적어 두라고 한다". 근거는 `:323`·`:406`·`:408`·`:426`. 뒤따르는 "방법론서가 자기 처방의 폐기 시점을 문서화하는 건 흔치 않다"는 그대로 성립한다.
- 부기: 검증 기록 7번이 "부록 D의 **마지막 절**이" → "**원리 4**가"를 교정한 것과 같은 부류(검증 가능한 위치 주장)다.

**72. "책의 마지막 문장" — 발행본에서 승계된 위치 주장이 거짓**
- 서지/출처: `closing_마치며.md:26` — "규모는 달라도 시작 방식은 같습니다. 규칙을 외부화하고, 반복되는 실수를 시스템 차원에서 방지하는 것입니다. **결국 이 책이 말하고자 한 것도 하나였습니다. 하네스 구축이란 완벽한 문서를 만드는 일이 아니라, 같은 실수를 반복하지 않는 환경을 만드는 일이라는 점입니다.**" 그 뒤: `:28` `> "챗봇과 싸우지 말고, 하네스를 설계하라." — 미첼 하시모토` → `:30` "이 책이 그 설계를 시작할 개념과 감각을 전해드렸기를 바랍니다. 이제 여러분의 현장에 직접 적용해 보시길 바랍니다." → `:32` 서명.
- 확인 방법: `cat -n closing_마치며.md | tail -20`으로 「마치며」 말미를 행 번호와 함께 실물 출력해 인용 문장 뒤에 인용 1건 + 맺음 2문장 + 서명이 남아 있음을 확인.
- 본문 대비 정합성: **원고가 근거와 어긋남(경미).** 인용된 문장은 책의 **최종 요약**이지 마지막 문장이 아니다. 이 노트 근거 E도 하시모토 쪽을 "**책의 마지막 인용**"으로 따로 적어 두어 둘을 구분한다. 발행본(1~4차 전부)이 놓친 지점이다.
- 교정 내용: "책의 마지막 문장이" → "책이 스스로 내놓는 마지막 요약이". `:26`이 스스로 "결국 이 책이 말하고자 한 것도 하나였습니다"로 규정하므로 근거와 일치한다. 블록인용 자체는 손대지 않았다(축자 유지).

### P. 5차 라운드에서 확정한 것 (7건 묶음)

**73. 블록인용 18건 전수 축자 재확인**
- 확인 방법: HTML 주석 제거 후 초안의 `> ` 인용 18건을 스크립트로 추출해 전사 저장소 전역 `grep -rF`로 1건씩 대조.
- 결과: **16건 즉시 축자 일치.** 불일치 2건은 모두 설명된다. ① "프롬프트는 설득에 의존하지만…" — 원문 `chapter_01:192`가 첫 문장만 `**…**`로 강조해 볼드 기호 때문에 `grep -F`가 빗나간 것이고, 기호를 제외하면 문자 단위 일치. ② "혼자 쓴 PR을 혼자 머지하는 구조입니다." — 전사본 `chapter_01:50`은 OCR 오탈자로 "**흔자** 쓴 PR을 **흔자** 머지(merge)하는"이며, 1차 검증 기록 11번이 이미 정정 인용이 타당하다고 판정한 항목이다. `(merge)` 병기 생략도 같은 판정 범위.
- 정합성: 확정. 발행본 15건이 전부 생존하고, 늘어난 3건은 4차 신규 채택 2건과 인라인→블록 승격 1건이다(76번).

**74. 인라인 인용부호 문자열 50건 전수 대조 — 70번 외 추가 이탈 없음**
- 확인 방법: 블록인용 행을 제외한 본문에서 정규식 `"([^"\n]{6,})"`로 인용부호 문자열 50건을 추출해 전역 `grep -rF`로 1건씩 대조. 70번을 적발한 방법을 표본이 아니라 **전수**로 확대한 것이다.
- 결과: 책 인용으로 귀속되는 문자열은 **전부 원문에서 확인**(정의·5축 예고·"이 장은 그 다섯 파일을…"·attribution·"여섯 번째 팀을 만들던 날"·"2부에서 본 여섯 단계…"·에어비앤비 재해석·"파일이 없으면 존재하지 않는다"·JSON 논거·"QA Phase는 만들지 않는다"·"이제 될 거예요"·"테스트를 수정·삭제하지 않았는가"·Vercel 결론·"무료 모델 업그레이드"·"무비용 교육"·파울러 명제·book-writer 이력 5항목·"이게 왜 있지?"·"이 책이 반복해서 강조하고 싶은 문장은 단 하나"·"결과를 결정하는 것은 모델 자체가 아니라 하네스였습니다"). 미일치 항목은 성격별로 전부 정당하다 — ⓐ 글의 제목·부제, ⓑ 각주의 영문 논문·기사 제목 5건(외부, 1차 25~31번 범위), ⓒ 필자 자신의 수사적 표현("어떻게 강제할 것인가", "이것도 할 수 있다", "이건 내 일이 아니다", "지금 이걸 불러야겠다", "더 정교한 것이 더 낫다", "50개면 충분하다", "무엇을 넣을까", "어디서 멈출까", "언젠가 이 명제가 약해질 것"), ⓓ 따옴표 중첩 때문에 겉꺼풀만 다른 1건("'모델의 성능이 부족하다'는 진단은 종종 오판" ← `appendix_b:119`의 `**"모델의 성능이 부족하다"는 진단은 종종 오판입니다**`, 내부 인용부호를 단따옴표로 바꾼 것이라 내용은 축자).
- 잔여 3건은 사실 오류가 아니라 표현 결정이므로 R절에 넘겼다.

**75. 신규 채택 인용 2건과 "헛침 해소" 문단의 사실 진술 — 독립 재확인 (4차 59·60번과 동일 결론)**
- 서지/출처: `chapter_04:58`("'설정'보다 '계약서'가 정확한 이유는 간단합니다. **설정 파일은 값을 저장하지만 계약서는 약속을 저장합니다.** description 한 줄이 호출 시점을 바꾸고 tools 한 줄이 부작용 범위를 바꿉니다."), `chapter_04:616`("**model·tools는 거버넌스 결정이지 성능 최적화가 아닙니다.** 두 축은 독립되어 있으며 model은 비용과 추론 등급을, tools는 권한 경계를 결정합니다. Write를 주지 않으면 본문을 수정할 수 없는 파일 수준 가드레일이 됩니다."), `chapter_04:236`(절 "가드레일 — 에이전트가 할 수 없는 것을 파일로 정하기": "프론트매터의 tools 필드가 그 역할을 합니다. 자연어 지시는 안전장치가 아닙니다. … 'Write 금지' … 라고 써두는 건 프롬프트일 뿐입니다. … 확률적으로 판단하는 시스템에서는 판단이 흔들리는 순간이 반드시 옵니다."), `chapter_04:252`("tools 필드는 '이 에이전트 하나'의 경계를 정하는 파일 수준 가드레일입니다.").
- 확인 방법: `sed -n '54,62p'`·`'232,258p'`·`'610,620p' chapter_04_에이전트_정의.md`로 세 구간 전문을 출력해 대조. 신규 인용 2건은 `grep -rn "설정 파일은 값을 저장하지만"`·`"거버넌스 결정이지"` 각 히트 1건으로 좌표 확정.
- 정합성: **두 인용 모두 축자 일치**(마침표 포함)하고 리서치 노트 근거 C(4장) `:70`·`:74`행이 적어 둔 좌표·문맥과 부합한다 — `:58`은 "왜 설정 파일이 아니라 계약서인가"의 답, `:616`은 그 장의 정리 문장이라 초안이 전개의 시작과 결론으로 배치한 것이 원서 구조와 맞다. **근거 사슬 게이트 통과.** 문단의 나머지 진술도 근거에 정확히 대응한다: 파일 수준 가드레일이 `tools` 전용인 점(`:236`·`:252`), `model`이 비용·추론 등급이라는 별개 축인 점(`:616`), "Write 금지는 프롬프트일 뿐"과 "판단은 언젠가 흔들린다"(`:236`). 4차 59번이 `model`을 가드레일에서 떼어낸 교정이 **원문 대조로 재확인된다** — 전역 `grep -rn "파일 수준 가드레일"` 히트 4건(`:252`·`:547`·`:567`·`:616`)이 전부 tools에 대한 서술이고 model을 가드레일로 부르는 문장은 저장소 전체에 없다.

**76. 재배치가 사실을 훼손하지 않았음 — 기계적 대조 5종 (독립 재수행)**
- 확인 방법: HTML 주석 제거 후 발행본↔초안을 직접 비교하는 스크립트를 새로 작성해 실행.
- 결과: ① **블록인용** — 발행본 15건 중 **post-only 0건**(전부 생존, 한 글자도 다르지 않음), draft-only 3건 = 신규 2건(75번) + 승격 1건(78번). ② **숫자 multiset** — `draft − post = {'05': 1}`, `post − draft = {'4': 1, '13': 1, '50': 1}`. 초안에만 있는 숫자는 Part 05 하나뿐이고, 빠진 셋은 문장 병합의 결과다("뒤의 13개 장과 부록 4편" → "나머지 전부가"는 도입부 계수의 중복 제거, `50`은 역산 서술을 한 절로 합치며 `"50개면 충분하다"` 한 번이 줄어든 것으로 상한값 자체는 본문에 그대로 있다). **새 숫자·유실된 사실 0건.** ③ **표** — 17행(3개 표) 문자 단위 동일. ④ **각주** — 정의 7개 문자 단위 동일. ⑤ **코드블록** — 1개 동일(bash 2줄, 주석 포함).
- 정합성: 확정. ④가 이번 라운드에서 외부 재조회를 생략한 근거다.

**77. 도입부 "목차 5부" 서술 — 독립 재확인, 오도 없음**
- 서지/출처: 목차 Part 표기 `00_front_matter.md:182`(**Part 01 하네스란 무엇인가**), `:214`(**Part 02 하네스 구성하기 — 에이전트·스킬·오케스트레이터**), `:327`(**Part 03 하네스 스킬로 구성하기**), `:445`(**Part 04 하네스 실전편**), `:532`(**Part 05 부록**). 산문 `:149` "본문은 총 4부, 14장으로 이루어져 있습니다. 추가 부록은 총 4개입니다." Part별 목적 산문 `:151`(1부 = "'왜 하네스인가를 납득시키는 파트"), `:153`(2부 = 세 기둥), `:159`(3부 = "2부의 단계를 자동화하는 메타스킬" + 등록·drift 유지), `:161`(4부 = "4개의 도메인 사례"), `:163`(부록 4편 + "본문이 원리를 설명한다면 부록은 레시피에 가깝습니다").
- 확인 방법: `grep -n "Part 0" 00_front_matter.md`로 Part 표기 5건 전수 추출(1차 8번·4차 61번의 독립 재수행). `sed -n '145,168p'`로 「이 책의 구성」 산문 전문을 출력해 초안의 다섯 요약구를 1:1 대조.
- 정합성: **정확.** 다섯 요약구가 각각 Part 01~05의 산문 설명과 대응하고, "마지막 덩어리까지 번호를 받아 Part 05로 적혀 있다"는 `:532`의 인쇄 사실이다. 산문(`:149`)의 "4부+부록 4개" 계수와의 불일치를 밟지 않는 장치도 그대로 확인된다 — 초안이 "목차 쪽 사실부터 적어 두면"으로 범위를 목차로 한정하고, "마지막 덩어리**까지** 번호를 받아"라는 조사로 부록이 원래 파트가 아님을 함의하며, 바로 앞 문단이 "14개 장과 부록 4편"으로 산문 쪽 계수도 함께 싣는다. 제목의 대비도 목차로 한정돼 있어 책이 스스로 "5부"라 주장한다는 오해를 만들지 않는다.

**78. 292 귀속·"3명의 집중된 팀원" 이동·인용부호 3건 제거 — 독립 재확인**
- 서지/출처 및 확인 방법: `sed -n '424,448p' chapter_06_오케스트레이터.md`와 `sed -n '268,276p' chapter_08_아키텍처_패턴.md` 전문 출력.
  - **292 귀속**: `chapter_06:430`이 인용 끝에 `— 클로드 코드 내부 사고 사례, 오케스트레이션 패턴 가이드`를 붙이고, `chapter_08:271-273`은 "제가 직접 겪은 다음 예시를 보겠습니다."에 이어 **attribution 없이** 같은 인용을 재게재한다(조사도 6장 "9a990de8**이**" / 8장 "9a990de8**가**"로 다름). 초안 인용문은 6장 판과 일치하므로 "인용 끝에는 출처가 …로 붙어 있다"는 **초안이 싣는 그 게재분에 대해 참**이고, 8장은 경쟁 표기를 인쇄하는 게 아니라 표기를 생략하므로 충돌하지 않는다. 뒤따르는 추론도 "이 표기대로라면"으로 한정돼 사실 주장으로 승격되지 않는다. **확정 — "책은 …로 표기한다" 류로 일반화하지 말 것.**
  - **인용 이동**: `chapter_06:444` `> "3명의 집중된 팀원이 5명의 산만한 팀원보다 낫습니다." — 메타스킬 공식 SKILL.md`. 원서 문맥은 `:425`("각 팀원이 나머지 9명의 메시지를 수신하고 적절히 반응하려면 자기 컨텍스트 윈도의 상당 부분을 메시지에 빼앗깁니다. 즉, 실제 작업에 쓸 컨텍스트가 줄어듭니다.") → `:430`(292) → `:432`(큐 50개 상한) → 권장 팀원 수 표 → `:444`(인용) 순이다. 초안이 이 인용을 옮긴 자리("컨텍스트는 공공재이고, 팀원을 한 명 늘리는 건 나머지 전원의 몫에서 조금씩 떼어내는 일이다. 그래서 이 문장이 성립한다")가 바로 그 흐름이므로 **문맥 정합성이 발행본보다 개선됐다.** 축자성도 개선 — 발행본은 인라인이라 마침표가 빠져 있었고 초안 블록인용은 원문대로 마침표를 포함한다. **상한선 절로 되돌리지 말 것.**
  - **인용부호 3건 제거**: 문자열 검사로 세 문구가 발행본에는 있고 초안에는 없음을 확인(`책임을 나누고 실행 시점에 맞물린다` / `거짓 양성이 그대로 커밋되기 때문"` / `tools 한 줄이 만드는 파일 수준 가드레일` → 초안 전부 미검출). 남은 평서문은 필자 서술로 읽히며, ⓐ는 `chapter_03:36`의 "실행 시점**에만**"을, ⓑ는 `chapter_11:74`의 "검증 과정에서"를 되살려 원문에 오히려 가까워졌다. **3차 58번의 권고가 이행됐다.**
- 정합성: 세 항목 모두 확정. 다만 58번이 같은 부류로 지목하지 않았던 잔여 2건은 R절에 남겼다.

**79. 그 밖의 신규·변경 서술 전수 검토 — 71·72번 외 사실 이탈 없음**
- 확인 방법: 발행본↔초안을 문장 단위로 쪼개 초안 전용 107문장을 뽑아 육안 전수 확인하고, 사실이 걸린 진술은 원서 행으로 개별 대조했다.
- 개별 대조 결과(전부 정확): 최소 하네스 파일 5개 구성과 "이 장은 그 다섯 파일을 세 덩어리로 나눠 봅니다" `chapter_03:34` / 본문 7개 섹션에 '팀 통신 프로토콜'이 포함되고 `chapter_06:226`이 "4·5장에서 … '## 팀 통신 프로토콜' 섹션을 비워 두었습니다"로 회수 / 하네스 정의·5축·"이 다섯 축이 실제로 어떻게 구성되는지는 2부에서 따로 다룹니다"·범위 포함관계 `chapter_01:151`·`:155`·`:161`·`:170` / 다루지 않는 것(파인튜닝·RLHF·모델 아키텍처) `00_front_matter.md:146` / 메타스킬 443라인·500라인 상한·references 6개·1,706라인·약 4배 `chapter_07:109` / 4부 배치의 패턴 대응 4건 `chapter_08:423-429` 예고표와 일치 / "QA Phase는 만들지 않는다" + boundary-verifier Phase 2 상주 + 엔드포인트 완성 직후 검증 `chapter_12:198` / N×(N-1)/2·4명 6개·10명 45개·star→full mesh `chapter_06:207`·`:425` / 리더 우회의 이유가 성능이 아니라 병렬성 `chapter_06:61` / 변경 이력 **열 4개**와 "사유" 강조 `chapter_10:321`("열 순서는 날짜, 변경 내용, 대상, 사유로 정해져 있습니다")·`:323`("4개의 열 중 마지막 '사유'는 특히 중요합니다") / 사유 미기재 시 삭제 근거를 만들 수 없다·"이게 왜 있지?" `chapter_10:323` / ADR 삭제 조건 예시 `chapter_10:421`.
- 정합성: 확정. 나머지 차집합은 ⓐ 장 지칭 서두를 질문·수치·장면으로 바꾼 치환, ⓑ 소제목 교체, ⓒ 연결부 산문 압축, ⓓ 리플릿 고유명 제거(미해결 질문 9번을 더 멀리 회피하는 방향)로 사실 층위를 건드리지 않는다. **"패턴 커버리지 순이다"는 책이 명시한 문장이 아니라 필자 해석**이라는 4차 66번의 단서도 유효하다(예고표는 장별 패턴만 밝히고 순서의 이유는 밝히지 않는다) — 저자 진술로 귀속돼 있지 않으므로 유지.

### Q. 5차 라운드 확인 불가 (0건)

없음. 본문의 외부 인용(각주 7건)은 이번 판에서 변경되지 않아 1차 25~31번의 검증이 그대로 유효하고, 신규·변경 서술은 전부 원서 전사본으로 귀결됐다.

- **미해소로 남는 것은 1~4차와 동일하다:** 292개 에이전트 사건의 외부 검증(공개 문서 없음)과 **책 내부의 출처 표기 불일치**(`chapter_06:430` attribution vs `chapter_08:271` "제가 직접 겪은"). 초안은 이를 사실 주장으로 승격시키지 않고 6장 게재분의 표기를 그대로 옮기는 방식으로 처리하고 있어 발행에 문제가 없다. 미해결 질문 3번 참조.

### R. 잔여 — 사실 오류가 아니라 표현 결정 (editor·사용자 몫, 3건)

74번의 전수 대조에서 걸러진, **인용부호를 쓰지만 축자 인용이 아닌** 잔여 3건이다. 셋 다 발행본에서 승계됐고, 해당 명제의 축자 형태가 같은 글 안에 이미 블록인용으로 실려 있어 독자가 오인할 위험이 낮다. 3차 58번이 같은 부류를 "표현 결정"으로 분류한 선례에 따라 교정하지 않았다.

1. `"프롬프트는 설득, 하네스는 강제"에 처음으로 계산이 붙는 지점이다` — `chapter_01:192`의 압축. 축자 형태는 같은 글 앞부분에 블록인용으로 실려 있어 되받기(callback)로 읽힌다.
2. `책의 명제는 "모델이 아니라 하네스가 결과를 결정한다"이다` — 원문은 "결정합니다"(`00_front_matter.md:109`). "…이다" 구문에 맞춘 어미 변환이며 축자 형태는 글 앞부분 블록인용에 있다.
3. `"단일 리뷰어 모델이 두 관점을 한 번에 안정적으로 다룰 수 있는 역량에 도달했을 때."` — `chapter_10:421` 원문에는 문말 마침표가 없다. 인용 조각을 문장으로 닫기 위한 부호 추가이므로 의미 변화가 없다.

또한 `"테스트를 수정·삭제하지 않았는가"`(`chapter_14:252`)는 원문이 `- [ ] 테스트를 수정·삭제하지 않았는가(Anthropic 규칙)`로 출처 태그를 덧붙이지만, 이는 3차 50번이 복원한 Vercel의 `(정적 마크다운)`처럼 **주장 내용을 밝히는 주석이 아니라 귀속 표기**다. 4차 63번이 `— 메타스킬 공식 SKILL.md`를 인라인 대신 각주 `[^book]`로 처리한 판단과 같은 결로 생략을 유지한다.
